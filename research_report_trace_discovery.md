# 研究报告：不依赖追踪表的候选追踪关系发现（Trace Discovery）

> created: 2026-08-30
> 状态：方案设计（含原型实验实证）
> 配套探针脚本：`_scratch_candidate_discovery_probe.py`（临时，可复现第 3 节实验）

---

## 1. 问题定义

### 1.1 现状：追踪关系完全来自下游附录表

当前流水线的"选对"与"验证"是分离的，且**选对环节完全依赖下游文档末尾的追踪矩阵附录表**：

```
下游PDF ──find/parse_traceability_table──> TraceRelation列表   ← 追踪关系的唯一来源
                                              │
下游条目提取 + 上游ContentMap.resolve ────────┤
                                              ▼
                              TraceabilityRow（逆向矩阵行）
                                              │
                       text_matcher.verify_matrix / match_text_pair
                                              ▼
                          微块级着色验证（GREEN/BLUE/BLACK）
```

- 流水线 A：`core/traceability_matrix.py:111` `build_backward_matrix`，第 133-139 行若 `find_traceability_table` 失败**直接 raise**，流程无法进行。
- 流水线 B：`core/traceability_matrix.py:261` `build_backward_matrix_from_design`，同样以附录表为前提。

也就是说：**匹配引擎（`match_text_pair`）只回答"这两段文字像不像"，从不回答"这两条需求该不该配对"**。表缺了、表错了、表漏了，整个系统就失效或带病运行。

### 1.2 目标

在进入微块级匹配验证**之前**，增加一个**候选追踪发现（Candidate Trace Discovery）**阶段：完全不读附录表，仅根据上下游文档内容，为每条下游单元（需求条目或章节）找出候选的上游单元（条目或章节），并给出分数与证据。

三类要发现的配对形态（基准数据 `基准数据/Trace_Base.xlsx` 中均真实存在）：

| 形态 | 示例（基准） |
|---|---|
| 条目 → 条目 | `<DCS-SyRS004>` → `<RPS-SYS-RQ-008>` |
| 条目 → 章节 | `<DCS-SyRS001>` → 规格书 `3.2.2.1 F-SC1级要求` |
| 一对多 | `<DCS-SyRS005>` → `RQ-008` + `RQ-009` |

---

## 2. 可复用的现有资产

新阶段几乎不需要引入新依赖，现有模块可直接拼装：

| 资产 | 位置 | 复用方式 |
|---|---|---|
| 条目/章节提取 | `core/requirement_extractor.py` `extract_requirement_items` / `extract_sections` / `detect_document_type` | 下游、上游两侧统一"单元化"（含 `ContentMap`） |
| 跨前缀 ID 归一 | `core/traceability_matrix.py:220` `_id_core`（`FZSDCS34-SyRS005`→`syrs5`） | 仅用于发现结果的登记/索引，**不作匹配证据** |
| 嵌入模型 | `models/embedding_model.py` `encode` / `cosine_similarity_matrix`（bge-small-zh） | 语义通道，批量编码一次算完 |
| 字符级相似度 | `core/text_matcher.py:491` `_char_bigram_jaccard`、`:520` `_char_set_jaccard`、`:551` `_lcs_ratio`、`:437` `_normalize_for_char_compare` | 词汇通道，鉴别近似候选 |
| 微块分解 | `core/text_matcher.py:65` `decompose_text` | 条目→句子/短语级证据聚合 |
| 全局最优匹配 | `core/text_matcher.py:962` `_optimal_matching`（scipy 匈牙利） | 候选决策层的全局一致性约束 |
| 矩阵容器/输出 | `TraceabilityRow` / `TraceabilityMatrix` / `verify_matrix` / `excel_generator` | 发现结果直接注入现有流水线 |

---

## 3. 数据证据与原型实验

### 3.1 语料中的发现信号（探针实测）

对 5 份样本文档实测（2026-08-30）：

1. **ID 族结构**（仅作索引观察，不作匹配证据）：
   - 系统设计《总体方案》：`FZSDCS34-OTS0xx`(43) + `FZSDCS34-SyRS0xx`(34)；《仪控报警》：`ICADS0xx`(23) + `SyRS0xx`(11)。
   - 设计与需求文档存在编号形似的条目（`<FZSDCS34-SyRS005>` vs `<DCS-SyRS005>`），但**编号形似不构成追踪证据**（领域反馈：两者只是编号类似）。编号仅在关系经内容匹配确定后，用于登记与索引。
2. **正文裸编号**：设计文档正文出现 `SyRS005`/`SyRS008` 等裸编号（总体方案 34 处）；系统需求文档出现 `DCS-SyRS0xx` 裸编号 28 处。多为文档自身条目编号的行文提及，**同样不作为匹配证据**，仅作观察记录。
3. **章节引用极少**：全部文档中"见 X.X 节"类引用仅 1 处 → **章节级发现不能依赖显式引用，必须靠标题+内容相似**。
4. **上游异构**：《DCS设备技术规格书》为纯章节型（无 ID），《RPS系统需求规范书》为 ID 型 → 候选池必须"条目+章节"混装。
5. **下游也有可用章节骨架**：《DCS需求说明书》可提取 `3.1 安全功能分级要求 / 3.2 系统的功能要求 / 3.4.1 紧急停堆系统` 等章节（含少量噪声标题如"48 V直流电源系统"），粗对齐可行但需噪声过滤。

### 3.2 原型实验：纯内容发现的可行业绩线

探针实验（`_scratch_candidate_discovery_probe.py`）：下游 14 条 `DCS-SyRS0xx`，上游候选池 = RPS 13 条条目 + 两份上游文档的 15 个非空章节，**不使用任何追踪表信息**，混合打分 `0.75×条目级嵌入余弦 + 0.25×字符 bigram Jaccard`：

| 指标 | 结果 |
|---|---|
| top-1 命中（基准 14 条关系） | **10/14 = 71%** |
| top-3 命中 | **12/14 = 86%** |

**失败模式分析（决定方案设计方向）**：

| 失败案例 | 原因 | 对应解法 |
|---|---|---|
| `<DCS-SyRS002>` gold=`3.2.2.2 F-SC2级要求`，top1=`3.2.2.1 F-SC1级要求`（嵌入 e0.99 vs e0.96） | 三个 F-SC 章节是"同模板近似文本"，整条嵌入无法区分 | 词汇通道需 **IDF 加权的区分性 token 证据**（"F-SC2"一词定胜负），句子级证据聚合 |
| `<DCS-SyRS008>`/`<DCS-SyRS009>` gold=`RQ-010`（208 字，涵盖四个保护组）完全未进 top3 | 整条嵌入被长上游条目稀释；下游条目只覆盖上游的一个侧面 | **微块级证据聚合**（下游句子在上游中的最佳匹配 + 覆盖率），而非整条对整条单一分 |

结论：**纯"整条对整条"的相似度召回达不到生产要求，但作为召回通道已经 86% top-3；需要多通道 + 章节级粗对齐 + 块级证据聚合的系统性方案。**

---

## 4. 方案总体架构

新增 `core/candidate_discovery.py`，插入位置在 `build_backward_matrix*` 与 `verify_matrix` 之间。三层结构：

```
┌─ 通道层（recall，宁多勿漏，各通道独立打分；全部基于内容）────┐
│  A 词汇证据通道：char bigram Jaccard + IDF加权区分性token     │
│  B 语义证据通道：条目级嵌入 + 微块级证据聚合(max+coverage)     │
│  C 结构证据通道：章节级粗对齐(标题相似+章节内容聚合)           │
│  ✗ 编号形似/正文裸编号：不作匹配证据                          │
└──────────────────────────────────────────────────────────┘
                    ▼ 候选决策层（precision）
   章节级粗对齐先行（限制条目搜索空间）→ 内容多通道分数融合
   → 阈值 + margin 校验 + 全局一致性(匈牙利/互近邻)
   → CandidateRelation(top-k，带证据与置信度)
                    ▼ 接入层（编号在此登场：仅作登记索引）
   以 编号/章节号 登记 → 注入 TraceabilityRow → verify_matrix
   → 逆向/正向 Excel（增加"来源"列：table/discover/hybrid）
```

### 4.1 通道层设计（全部基于内容比较）

> 设计原则（领域反馈确认）：**编号形似 ≠ 存在追踪关系**。候选发现只依据内容
> （词汇 / 语义 / 章节结构三通道）；条目编号与章节号仅在关系确定后用于登记与索引。

**通道 A：词汇证据（鉴别近似候选的关键）**

- 全量：`_char_bigram_jaccard`（已有）。
- **IDF 加权区分性 token**：对候选池统计 token 文档频率，只对低频 token（如 `NASPIC`、`F-SC2`、`保护组`、`停堆断路器`）计算命中率。3.2 节失败案例证明：区分 `3.2.2.1` 与 `3.2.2.2` 靠的不是整体相似度，而是低频判别词。

**通道 B：语义证据（召回主力，需块级聚合）**

- 第一层：条目级嵌入余弦（`encode` 批量一次算完，14×28 规模毫秒级）。
- 第二层（关键改进）：对每对候选，把下游条目 `decompose_text` 到句子/短语级，计算每个下游微块与上游条目**全量微块**的最佳相似度，聚合成两个特征：
  - `block_max`：最佳句对相似度（捕捉"覆盖上游一个侧面"的情形，解 SyRS008/009 失败）；
  - `coverage`：相似度≥0.70 的下游微块占比（捕捉"部分覆盖"程度）。
- 计算量控制：先用条目级分召回 top-10，再仅对 top-10 做块级聚合精排。

**通道 C：章节级粗对齐（搜索空间压缩 + 结构证据）**

- 对下游与上游的章节集合做对齐打分：章节标题 `_char_bigram_jaccard` + 标题层级（`3.4.1`↔`3.4` 父子关系）+ 章内条目与对侧章节的条目级嵌入得分矩阵的聚合（"章节里住着谁"比"章节标题像不像"更可靠）。
- 产出：章节级候选对（本身就是要输出的"章节级追踪关系"），并作为条目级匹配的**先验**——同章节内的条目候选获得加成，跨章节不惩罚（只降权），避免粗对齐错误传导为硬过滤。

### 4.2 候选决策层

1. **分数融合**：`score = Σ w_c × channel_c`（词汇/语义/结构三通道，纯内容），初始权重建议 A:0.35 / B:0.45 / C:0.20，用 `Trace_Base` 金标调参。
2. **准入阈值 + margin**：`score ≥ θ_accept`（初值 0.55）收录；top1 与 top2 差 < margin（0.05）时标记 `ambiguous`，输出多条候选交由验证阶段/人工裁决。
3. **全局一致性**：
   - 下游条目默认只保留 1 条主关系（score 最高），top-k 作备选——符合"一条下游需求溯源到一个主要来源"的工程惯例；
   - 上游单元允许多条下游挂接（一对多，基准中 RQ-010/012 均如此），不做一对一硬约束；
   - 可选匈牙利全局分配（`_optimal_matching` 思路上移到条目级）处理系统性错配。
4. **无候选兜底**：某下游条目所有候选低于阈值 → 输出 `UNTRACED` 标记行（这本身就是有价值的审查产出："该条目在上下游文档间找不到来源"，是现在依赖表格模式完全发现不了的问题）。

### 4.3 数据模型与接入

```python
@dataclass
class CandidateRelation:
    downstream_id: str
    downstream_doc: str
    upstream_doc: str
    upstream_ref: str          # 条目ID 或 章节号(+标题)
    level: str                 # "item" | "section"
    score: float               # 融合分
    channel_scores: dict       # {"explicit":.., "lexical":.., "semantic":.., "structure":..}
    evidence: list[str]        # 命中的区分性token / 最佳句对摘录(纯内容证据, 不含编号)
    ambiguous: bool = False
    is_untraced: bool = False  # 兜底标记
```

接入方式（对现有代码侵入最小）：

- `build_backward_matrix*` 内部把"附录表解析出的 relations"替换/合并为"`discover_candidate_relations()` 的输出"，后续 `ContentMap.resolve` → `TraceabilityRow` → `verify_matrix` 全链路不变；
- `TraceabilityRow` 增加可选字段 `relation_source: str = "table"|"discover"|"hybrid"` 与 `candidate_score: float`，Excel 输出侧加一列展示；
- **三种运行模式**：
  - `table`（现状，默认）：有表走表，行为零变化；
  - `discover`：无表/忽略表，纯发现（本次方案主体）；
  - `hybrid`：以表为准，发现结果用于 ①补漏（表里没有但内容强匹配的关系，标记 `added`）②纠偏（表关系分数异常低时标记 `suspicious`）。hybrid 模式下发现通道的反向价值最大——**审查表本身的质量**。

### 4.4 与验证引擎的分工

发现阶段只回答"**谁跟谁配对**"（关系级），颜色判定仍完全交给现有 `match_text_pair`（微块级）。两阶段职责正交：

- 发现阶段输出的低置信候选，验证后可能整体 BLACK → 恰好暴露"表里声明了但内容对不上"的关系；
- 验证阶段的逐对输出（GREEN/BLUE 比例、coverage）可回喂发现阶段做二阶段精排（后续迭代项）。

---

## 5. 实施路线

| 阶段 | 内容 | 验收 |
|---|---|---|
| P1 骨架 | `core/candidate_discovery.py`：单元化 + 通道B/C + 条目级 top-k `CandidateRelation` 输出 + CLI 探针 | 流水线 A 金标 top-3 ≥ 90% |
| P2 精度 | 通道C 章节粗对齐 + 块级证据聚合 + IDF 词汇通道强化 + 阈值/margin | top-1 ≥ 90%，无"近似章节混淆"类错误 |
| P3 接入 | 三模式接入 `build_backward_matrix*` / `app.py`（侧边栏模式选择）+ Excel 增列 + 章节级关系 sheet | 流水线 A、B 全链路可跑通 |
| P4 调优 | hybrid 模式补漏/纠偏报告；编号登记一致性核对；噪声章节过滤强化 | 对 `Trace_Base` 全量 16 条关系零漏报（hybrid） |

性能预估：候选发现全程只需**一次条目级批量嵌入**（数十条 × 数十候选，秒级）+ top-10 的块级精排（复用 `_warmup_models` 批量预热思路），不引入 NLI 开销（NLI 只在验证阶段用）。

---

## 6. 评估方案

- **金标**：`基准数据/Trace_Base.xlsx` 逆向矩阵 16 行（14 个下游条目）；流水线 B 可从现有输出抽取附录表关系构成临时金标。
- **指标**：关系级 top-1 / top-3 recall、伪关系率（发现的错误关系数 / 总输出）、`UNTRACED` 召回（人为删除表中关系测试）。
- **对照**：table 模式（现状）vs discover 模式 vs hybrid 模式在同一金标上的差异表，进 `验证评估/`。

## 7. 风险与开放问题

1. **阈值迁移性**：θ/w 在 16 条金标上调出，换文档族可能漂移 → 阈值全部进 `config.py`，margin/ambiguous 机制保证"不确定就多输出候选"而非硬错。
2. **编号不可作证据（设计原则）**：编号形似（`FZSDCS34-SyRS005` vs `DCS-SyRS005`）只是编号类似，且跨文档前缀/零填充差异大、不同族编号还可能撞号，因此编号仅用于关系确定后的登记索引，绝不参与匹配打分——这正是"内容优先"设计的直接原因。
3. **章节噪声**：下游已见伪标题（"48 V直流电源系统紧急操作台"、"0 项目策划"）→ 章节对齐需复用并强化 `extract_sections` 的 TOC/噪声过滤。
4. **长条目一对多**：上游 733 字的 `RQ-008` 对多条下游——块级聚合的 `coverage` 方向性要注意（下游覆盖上游低是正常的一对多，不扣分；上游覆盖下游低才可疑）。
5. **多份下游文档**（流水线 B 三份设计文档）：发现按 `(doc, item)` 分组执行即可，架构无需特判。

---

## 8. 算法层详细设计（逐步实现）

> 本章给出 §4 方案的落地细节：数据结构、公式、伪代码、参数初值与边界处理。
> 原则回顾：**只按内容匹配（词汇 / 语义 / 章节结构三通道），编号与章节号仅作登记索引**。
> NLI 完全不参与发现阶段（只在验证阶段使用），发现阶段只有嵌入与字符级计算。

### 8.0 流水线总览

```
单元化 ──► 章节对齐 ──► 粗筛(top-K) ──► 精比(块级证据+稀有词) ──► 裁决 ──► 登记
 §8.1      §8.2        §8.3           §8.4                  §8.5    §8.6
```

### 8.1 单元化

**数据结构**（新模块 `core/candidate_discovery.py`）：

```python
@dataclass
class Unit:
    unit_id: str            # doc + '#' + key
    doc: str
    kind: str               # "item" | "section"
    key: str                # 登记键(条目编号 或 "章节号 标题")，仅登记用
    content: str
    span: tuple[int, int]   # 在文档全文中的字符区间(包含图/先验计算用)
    depth: int              # section=章节号级数, item=0
    order: int              # 文档内顺序
```

**提取改动（向后兼容）**：`RequirementItem` / `SectionItem` 增加可选 `span` 字段——
两个提取函数内部本就持有正则位置，只是没存。

**候选池构成**：
- 下游：ID 型文档取条目；章节型文档取章节。
- 上游：条目 ∪ 章节（章节须内容 ≥30 字），因上游可能是条目型（RPS）或纯章节型（设备技术规格书）。

**章节噪声过滤（序号骨架校验）**：按文档序扫描章节号，保留满足"单调骨架"的
（同级递增或降级为子级）；`1.3` 之后出现 `48`、`0` 这类破坏单调性的伪标题剔除。

**包含图**：`contains(a,b) = span_a ⊇ span_b`（同全文坐标系）。用于：
条目-章节双重命中时的"具体性折叠"（§8.5）与章节先验（§8.2）。
ID 型上游文档中，章节 "5 结构要求" 会包含条目 `RQ-010..013`——这正是探针实验中
"5 结构要求" 挤进第二名的原因，必须靠包含图消解。

### 8.2 章节对齐（先于条目级，产出先验 + 章节级关系）

```
for (Ds, Us) ∈ 下游章节 × 上游章节(均内容≥30字):
    t = bigram_jaccard(归一化(Ds标题), 归一化(Us标题))
    b = cos(E[Ds正文], E[Us正文])
    score(Ds,Us) = 0.5·t + 0.5·b
accept(Ds,Us) ⇔ 互为最优 且 score ≥ θ_sec(0.55)
次选: 第二名与最优分差 ≤ 0.10 也收录(下游一章可源自上游多章)
```

**产出**：
- 章节级候选关系（本身即交付物；下游为章节型文档时就是主关系）；
- 先验表：`prior(d,u) = 1.0`（d 的父章节与 u 或 u 的父章节对齐）/ `0.5`（仅祖父层级对齐）/ `0`。
  只加分不拦人，粗筛/精比仍对全部上游开放。

### 8.3 粗筛（召回 top-K，含长度不对称处理）

**单元向量（长文分段均值）**：内容 ≤300 字整条编码；超长按句子边界切 ≤300 字分段，
`E(u) = L2归一( Σ_c len_c·e_c / Σ_c len_c )`。解决 bge-small-zh 512 截断导致长条目失真。

**长度不对称问题（一侧很长、一侧很短但确有追踪关系）**：
长单元是多主题文本，其整条向量是各主题的"质心"；短单元只讲其中一个主题。
质心与单主题向量的余弦被系统性低估——对称打分(整条余弦/二元组Jaccard)会漏掉真对。
（探针实证：SyRS008/009 短条 vs 上游 208 字多主题条目，整条比对双双跌出前三。）

**不对称池化打分（核心解法）**：

```
seg(x)  = 句子级块(≥4字)，句向量全局缓存
S       = cos(句向量矩阵)                # 长侧逐句 × 短侧逐句(两侧都取句级亦可)
peak    = max S                          # 最佳句对(短侧实现长侧某主题的直接证据)
cover_s = Σ_{i∈短侧, b_i≥0.60} len_i / Σ_{i∈短侧} len_i   # 短侧被长侧承接的比例
sem_asym = max( 整条余弦, 0.4·peak + 0.6·cover_s )
```

要点：
- **只考核短侧覆盖率**：长侧剩余内容无着落不扣分(一对多合法)，
  "下游只实现上游一个侧面"的真对不再被稀释；
- **方向无关**：无论长的是上游还是下游，一律长侧分段、短侧整条；
- **词汇通道同步改造**：对称的字符二元组 Jaccard 在长度差大时同样被压低，
  改用**短侧包含度**(短侧二元组出现在长侧中的比例)替代 Jaccard；
  稀有词"短侧落点率"本身与长度无关，直接保留。

**误报防线（max 池化的风险控制）**：
分段相似度按稀有度加权(通用句权重趋零)；峰值取前二三句对均值而非单一最大；
纯峰值命中的对必须有稀有词佐证；命中块合计长度须达短侧一定比例。

**层级回卷（超长单元的检索子块）**：内容超过约 400 字的单元，将内部段落登记为
检索子块；子块命中回卷记到父单元(证据标"经子块命中")，登记粒度不变，
长单元可被短对命中。与 §8.1 包含图共用一套区间关系。

**粗筛流程**：

```
for u in 全部单元: E[u], seg[u] 一次性编码并全局缓存
for (d,u):
    sem_asym 按上式
    lex_asym = 0.6·短侧稀有词落点率 + 0.4·短侧二元组包含度
    c[d,u] = 0.7·sem_asym + 0.3·lex_asym
cand[d] = top-K(c[d,·]), K=10, 下限 c≥0.35(不足3个保底取3)
```

复杂度：句向量缓存后，全量对的不对称打分只是几次分块矩阵乘(数十单元语料毫秒级)；
更大语料改两级——整条分取宽候选(约20)再对候选做不对称复评。

### 8.4 精比（对 top-K 每对，块级证据 + 稀有词）

**(a) 句级语义聚合**（解"长上游稀释"）：

```
sent(x) = decompose_text(content) 中 level≤1(句子级) 且 len≥4 的块
S = cos(E[sent[d]], E[sent[u]])        # 句向量按文本哈希全局缓存
b_i = max_j S[i,j]                      # 下游句 i 的最佳落点
cover     = Σ_i len_i·1[b_i≥0.60] / Σ_i len_i      # 下游内容"有着落"比例
mean_best = Σ_i len_i·b_i / Σ_i len_i              # 长度加权平均落点
peak      = max S                                   # 最佳句对(下游实现上游某侧面)
fine_sem  = clip(0.5·cover + 0.3·mean_best + 0.2·peak, 0, 1)
up_cover  = (诊断量, 不进分) 上游句被下游覆盖比例——一对多时低属正常,
            用于区分"条目级 vs 章节级"关系与证据展示
```

**(b) 稀有词证据（IDF 加权，解"同模板章节混淆"）**：

```
tokens(x) = ASCII技术词 [A-Za-z0-9][A-Za-z0-9\-/]{1,} ∪ 汉字2-gram   (归一化后,
            大小写/连字符/全半角统一, F-SC-2 ≈ F-SC2)
df[t] = 候选池中含 t 的单元数;  rare(x) = {t: df[t] ≤ max(2, N//10)}
w(t) = ln(N / df[t])
P = Σ_{t∈rare(d)∩tokens(u)} w(t) / Σ_{t∈rare(d)} w(t)     # 下游稀有词落点率
R = Σ_{t∈rare(u)∩tokens(d)} w(t) / Σ_{t∈rare(u)} w(t)     # 上游稀有词被命中率
idf_f1 = 2PR/(P+R)   (分母 0 则取 0)
s_lex = 0.7·idf_f1 + 0.3·bigram(d,u)
```

`F-SC1/F-SC2/F-SC3` 各 df=1、权重最高，一个稀有词即可分离同模板章节——
探针中 SyRS002 误配 3.2.2.1 的问题由此修复。

### 8.5 裁决

```
s_sem = (1/3)·粗筛嵌入分 + (2/3)·fine_sem
F(d,u) = 0.45·s_sem + 0.35·s_lex + 0.20·prior(d,u)

main = argmax_u F(d,u)
若 F(main) < θ_accept(0.55):
    → UNTRACED 行(附 top-3 参考候选与证据)
否则:
    ① 具体性折叠: main 为章节且其内部某条目 u' 满足 F(u') ≥ F(main)−0.05
       → 改登记条目 u'(更具体);
       若 ≥2 个互不重叠条目均过阈值 → 保留章节级关系(覆盖广, 章节更合适)
    ② 存疑: 存在 u'' 与 main 无包含关系 且 F(u'') ≥ F(main)−0.05
       → ambiguous, 输出 ≤3 条候选
    ③ 一对多: u' 与 main 区间无重叠 且 F(u') ≥ max(0.55, 0.85·F(main))
       → 追加为次级上游关系, 上限 3
置信度: high(F≥0.70 且 margin≥0.08) / medium / low(UNTRACED 或 ambiguous)
```

全局一致性默认"轻量"：上游被多条下游命中是合法的一对多，不做强制再分配；
仅当多条下游挤向同一上游、且其他候选仅差毫厘时标记复核，不自动改判。

### 8.6 登记与接入

- 关系确定后按 `Unit` 直接取内容构建 `TraceabilityRow`——**不再走六级模糊解析**，
  从根上消除串稿面（发现阶段已持有内容，无需按编号反查）。
- Excel 增列：来源(table/discover/hybrid)、置信度、证据摘要(命中稀有词 top3 +
  最佳句对摘录)、存疑标记。
- hybrid 差集规则：发现−表=added(表漏)；表−发现且该下游另有强候选=suspicious(表错)；
  交集且 F 高=confirmed。

### 8.7 参数初值（全部入 config.py，金标调参）

| 参数 | 初值 | 用途 |
|---|---|---|
| K / c 下限 | 10 / 0.35 | 粗筛保留数与准入下限 |
| 分段长度 | 300 字 | 单元向量分段编码 |
| 句子最小长 | 4 字 | 句级证据过滤 |
| τ_c | 0.60 | cover 的落点判定线 |
| θ_sec / 次选差 | 0.55 / 0.10 | 章节对齐 |
| θ_accept / margin / 一对多下限 | 0.55 / 0.05 / 0.85×main | 裁决 |
| 权重 | sem 0.45(粗:精=1:2) / lex 0.35(idf:bigram=7:3) / prior 0.20 | 融合 |
| 稀有词 df 上限 | max(2, N/10) | IDF 词汇通道 |
| 章节内容下限 | 30 字 | 候选池过滤 |

### 8.8 复杂度与缓存

编码：每单元一次 + 每句子一次（文本哈希缓存），全程一次批量前向；
精比仅对 N_ds×K 对做 cached 向量点积；无 NLI。整体在秒级。

### 8.9 消融验证（P1/P2 验收口径）

对 Trace_Base 16 行金标做逐组件消融：粗筛only → +稀有词 → +块级精比 → +章节先验，
预期分别修复 SyRS002 类（稀有词）与 SyRS008/009 类（cover/peak）失败；
另做留一法（逐条抹除金标关系）验证 UNTRACED 场景的召回。
