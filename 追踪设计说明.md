# 核仪控工程文档追踪验证系统 — 详细设计说明

> 版本：2.0　日期：2026-09-07
> 适用系统：核仪控工程文档追踪验证系统（Trace\_NL）
> 覆盖范围：PDF 解析 → 需求提取 → 候选追踪发现 → 追踪矩阵构建 → 微块匹配验证 → Excel 报告输出的全链路设计
> 状态：已实现并通过三条流水线金标回归验证

***

## 目录

1. [系统概述](#1-系统概述)
2. [端到端处理流程](#2-端到端处理流程)
3. [PDF 解析层（五层结构化管道）](#3-pdf-解析层五层结构化管道)
4. [需求内容提取层](#4-需求内容提取层)
5. [候选追踪发现层](#5-候选追踪发现层)
6. [追踪矩阵构建层](#6-追踪矩阵构建层)
7. [微块文本匹配验证层](#7-微块文本匹配验证层)
8. [模型层](#8-模型层)
9. [Excel 报告生成层](#9-excel-报告生成层)
10. [性能优化设计](#10-性能优化设计)
11. [关键参数总表](#11-关键参数总表)
12. [工程结构与运行入口](#12-工程结构与运行入口)
13. [边界与后续方向](#13-边界与后续方向)

***

## 1. 系统概述

### 1.1 系统定位与目标

在核仪控工程领域，需求与设计文档之间存在严格的"追踪关系"：每一条下游内容（系统需求、系统设计条目）都应能溯源到上游内容（用户需求、系统需求）。人工逐条核对成本高且易漏，本系统的目标是：

- **自动构建追踪矩阵**：从上下游 PDF 文档中提取条目/章节，建立"下游 ← 上游"的逆向追踪关系，并可反转为正向矩阵；

- **自动验证内容一致性**：对每一条配对关系做短语级文本匹配，逐字着色（绿=完全一致、蓝=语义匹配、黑=未匹配），直观呈现"下游是否如实承接上游"；

- **不依赖人工维护的追踪表**：支持按内容本身自动发现候选追踪关系，表格缺失、遗漏、错误时系统仍能工作，并可对表格做补漏与纠偏。

### 1.2 文档层级与追踪关系

系统面向四级文档层级，形成两条标准验证流水线：

| 层级      | 角色              | 示例                             |
| ------- | --------------- | ------------------------------ |
| 用户需求    | 最上游             | 用户需求规范书（章节型）                   |
| 系统需求    | 中游（对上是下游，对下是上游） | 系统需求规范书（条目型，如 `<DCS-SyRS001>`） |
| 系统设计    | 下游              | 各专业设计文档（章节型 + 追踪附录）            |
| 软件/硬件需求 | 更下游             | （预留扩展）                         |

- **流水线 A**：系统需求（下游）→ 用户需求（上游），单份下游对多份上游；

- **流水线 B**：多份系统设计文档（下游）→ 单份系统需求（上游），每份设计文档有自己的追踪附录表。

### 1.3 总体架构

系统按"解析 → 提取 → 发现 → 构建 → 验证 → 报告"分层组织，各层职责正交：

```
┌────────────────────────────────────────────────────────────────────┐
│ 输入：上下游 PDF 文档                                                │
└──────────────────────────────┬─────────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│ ① PDF 解析层（双引擎 + 五层管道）                                    │
│    文本层判别 / 原子元素提取 / 版面分析 / 表格·标题·列表·阅读顺序      │
│    产物：分页正文文本、追踪关系表、结构化文档树                        │
└──────────────────────────────┬─────────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│ ② 需求内容提取层                                                    │
│    文档类型判别 / 条目提取 / 章节提取 / 内容清洗 / 内容映射            │
│    产物：条目 ID→内容、章节号→内容 的多策略索引                        │
└──────────────────────────────┬─────────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│ ③ 追踪矩阵构建层（三模式）                                           │
│    table：解析追踪关系表        discover：内容候选发现                 │
│    hybrid：表为准 + 发现补漏/纠偏                                    │
│    产物：追踪矩阵（每行 = 一条上下游配对 + 元数据）                    │
└──────────────────────────────┬─────────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│ ④ 微块匹配验证层                                                    │
│    三级微块分解 / 嵌入相似度 / 最优匹配 / 分类判定 / 子短语扫描        │
│    产物：逐字着色结果（绿/蓝/黑 TextRun）与整体类别                    │
└──────────────────────────────┬─────────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│ ⑤ Excel 报告层                                                      │
│    富文本着色 / 合并单元格 / 发现层元数据列 / 正逆向矩阵               │
└────────────────────────────────────────────────────────────────────┘
```

其中"③ 追踪矩阵构建"的 discover/hybrid 模式依赖**候选追踪发现层**（第 5 章），它在全量上下游单元之间按内容发现配对关系，是本系统区别于"查表工具"的核心能力。

### 1.4 三种运行模式

| 模式                 | 行为                                  | 适用场景             |
| ------------------ | ----------------------------------- | ---------------- |
| **表格模式 table**（默认） | 解析下游文档附录中的追踪关系表，按表配对验证，行为与传统模式完全一致  | 追踪表完整可信时的常规验证    |
| **发现模式 discover**  | 完全忽略追踪表，按内容在全量上下游单元间发现候选关系          | 表格缺失/不可信，或需要独立复核 |
| **混合模式 hybrid**    | 以表为准，发现结果用于补漏（added）与纠偏（suspicious） | 表格大体可信但可能有遗漏/错误  |

模式通过统一入口参数切换（命令行参数或界面下拉框），三种模式共享同一套解析、提取与验证基础设施。

***

## 2. 端到端处理流程

### 2.1 流水线全景

以流水线 A（系统需求 → 用户需求）为例，discover 模式下的完整执行序列：

```
 1. 并行预解析     对下游 + 所有上游 PDF 并行做正文提取，结果灌入缓存
 2. 单元化         下游按文档类型切分为条目/章节单元；上游条目与章节都取
 3. 向量编码       短单元整体编码；长单元句级编码后按长度加权聚合成单元向量
 4. 通道预计算     语义/词汇/稀有词/锚点/词频 五张打分矩阵 + 包含图 + 章节降权掩码
 5. 并集召回       每通道 Top15 + 粗排融合 Top30 的并集，构成精比候选集
 6. 句级精比       仅对候选集做不对称句级语义聚合
 7. 门控融合排序   基础分(0.5/0.2/0.2/0.1) + 锚点门控抬升 + 章节降权
 8. 具体性折叠     伞形章节 → 唯一高分内部条目
 9. 裁决           接受/未溯源/一对多/存疑 → 每个下游单元一条裁决
10. 矩阵行构建     裁决转矩阵行（主关系 + 次级关系 + 未溯源参考候选）
11. 全矩阵预热     批量编码 + 批量 NLI 推理，预热模型缓存
12. 逐行微块匹配   三级分解 → 嵌入相似度 → 匈牙利最优匹配 → 分类着色
13. Excel 生成     富文本着色、合并单元格、发现层元数据四列
14. 正向矩阵       由逆向矩阵反转为上游视角的独立报告
15. 基准对比       与人工标注的基准 Excel 逐单元格比对（回归验证）
```

table 模式下第 2~9 步替换为"解析追踪关系表 → 按下游 ID 分组 → 解析引用填充内容"；hybrid 模式在 table 流程之后追加发现层做补漏/纠偏。其中第 2~9 步即候选发现层内部的步骤 1~12（完整算法设计见第 5 章），第 10 步见 6.2 节，第 11~12 步见第 7 章。

### 2.2 两条流水线的差异

| 环节            | 流水线 A（系统需求→用户需求）            | 流水线 B（系统设计→系统需求）                 |
| ------------- | --------------------------- | -------------------------------- |
| 下游文档          | 1 份（条目型）                    | 多份（各专业设计文档，章节型为主）                |
| 上游文档          | 多份（用户需求等，章节型）               | 1 份（系统需求，条目型）                    |
| 追踪表结构         | Type A（下游 ID → 上游文档 + 上游引用） | Type A/B 两种（B 为"本文档设计标志号→上游条目号"） |
| discover 单元化  | 每份上游独立单元化后合并                | 每份设计文档独立跑一遍发现，序号续编               |
| Excel 分 sheet | 单文档单 sheet                  | 按设计文档名拆分多 sheet                  |

***

## 3. PDF 解析层（五层结构化管道）

### 3.1 双引擎与适配器

系统同时维护两套解析引擎，由适配器统一调度：

| 引擎                      | 定位      | 特点                                                   |
| ----------------------- | ------- | ---------------------------------------------------- |
| 新引擎（`pdfparser` 五层管道）   | 主引擎     | 结构化提取：章节层级树、列表树、表格网格、图片资产；正文质量高（页眉页脚/图注/表格不污染正文）     |
| 遗留引擎（`core/pdf_parser`） | 兜底与表格解析 | PyMuPDF 提取正文 + pdfplumber 提取表格；追踪关系表的查找与解析始终复用此引擎的函数 |

适配器规则（环境变量 `TRACE_NL_PDF_BACKEND`）：

- `auto`（默认）：优先新引擎，解析失败自动降级遗留引擎；

- `new`：强制新引擎，不降级；

- `legacy`：强制遗留引擎（行为与集成前完全一致）。

适配器对外接口与遗留引擎完全一致（分页正文、全文、表格），下游模块无感知。新引擎的解析结果（结构化文档树 + 分页文本）按 PDF 指纹做双层缓存，降级路径透明。

### 3.2 L0 预处理：文档类型判别

**目标**：在昂贵解析前判断文档是否"有真实文本层"，扫描件直接走 OCR 通道报告而不做无效解析。

**算法**：抽样检测（最多抽 10 页，按总页数等间隔）：

1. 逐页提取纯文本，统计非空白字符数；
2. 单页字符数 < 60 判为"疑似扫描页"；
3. 文本页占抽样页比例 ≥ 70% 且总字符数 > 0 → 判定有文本层；
4. 同时检测加密文档（需要密码），给出明确报告。

### 3.3 L1 原子元素提取

**目标**：把每页 PDF 内容还原为三类原子元素，作为后续所有结构分析的原材料。

| 元素  | 内容                                      | 提取方式                                          |
| --- | --------------------------------------- | --------------------------------------------- |
| 文本行 | 文本、包围盒、字体名、字号、粗体/斜体、颜色、是否 CID 复合字体、绘制序号 | 逐字符流重组（rawdict），行内多 span 拼接，字体属性取"最长 span"为代表 |
| 图像块 | 包围盒、对象引用号（xref）、绘制序号                    | 图像块按包围盒中心距离最近匹配回填 xref（距离 < 25pt）             |
| 矢量线 | 类型（水平线/垂直线/曲线/矩形）、包围盒                   | 解析绘图指令：直线按横纵坐标差 < 0.8pt 分类，矩形单列               |

**关键设计——绘制序号（paint\_order）**：文本块与图像块在内容流中按产出顺序统一编号，天然近似"绘制顺序"。它是后续阅读顺序排序的稳定性锚点，也是"图文中断点"检测的依据。

**字体判定的鲁棒性**：粗体判定不依赖单一标志位——除渲染标志外，字体名含 bold/black/hebo/demi/semibold 等关键词（覆盖 LaTeX 的 Computer Modern 系）同样判粗体；斜体同理。中文字体常见的 CID/Type0 复合字体单独标记，供编码兜底。

### 3.4 L2 版面分析：行 → 段 → 块

**目标**：把散乱的文本行聚合成有意义的版面块（段落、图题、页眉页脚、图片、结构图区域），并做初步分类。

#### 3.4.1 行聚类成段

逐页按（绘制序号, 纵坐标, 横坐标）排序后扫描，满足任一条件即断段：

| 断段条件   | 阈值                            | 捕捉的版面信号                                 |
| ------ | ----------------------------- | --------------------------------------- |
| 垂直间距过大 | 行距 > 1.6 × 行高                 | 空行、段间距                                  |
| 水平起点偏移 | 起点差 > 32pt                    | 缩进/对齐变化（32pt 容差覆盖中文"首行缩进 2 字符"≈30pt）    |
| 字号突变   | 字号差 > 1.2pt                   | 标题与正文的字号差异                              |
| 字体族突变  | 族不同 + 字号相近 + 短行 + 行距 > 0.4×行高 | 中文标题用楷体/黑体而正文仿宋的场景（排除"一是/二是"等中文列举引导句误断） |

段落的字体签名取块内**众数字号**（避免混合块被平均值带偏），签名 =（字体名, 众数字号, 是否粗体）。

#### 3.4.2 强制断段的特殊行

以下行不参与常规聚类，各自独立成块：

- **页眉/页脚行**（见 3.4.3）；

- **符号行**：非宋体/Times 字体且长度 ≤ 3 字符（Wingdings 项目符号），符号行与后续内容文本行强制分属两块——否则 bullet 标记会被并入正文导致列表结构丢失；

- **图题/表题行**："图/表/Figure/Fig./Table + 编号"开头且短小；

- **编号标题行**：多级编号（3.1 / 3.3.2）、"第X章/节/部分"、"附录A"；

- **短标题行**：前一行 ≤ 25 字符、不以标点结尾，且当前行明显更长 → 前一行是无编号标题。

#### 3.4.3 页眉/页脚识别（行级、跨页重复 + 位置稳定）

页眉页脚若在块级识别，常与下方标题粘连而失效，因此下沉到**行级**：

1. 统计每个短行（≤ 60 字符、非纯符号）在全文的出现次数与每次出现的纵坐标；
2. 出现次数 ≥ max(3, 页数 × 0.35) **且** 纵坐标极差 ≤ 20pt → 判为页眉/页脚。

"位置稳定"条件是关键防线：跨页重复出现的正文列表项（如多个章节共有的句尾短语）虽然重复次数高，但每次纵坐标差异大，不会被误杀；纯符号行（"-"、"•"）排除，避免列表项标记被当页眉剔除。此外，遗留引擎还保留一组页眉页脚正则（版本行、页码行、密级行、文档编号行），覆盖"页码每页不同"这类跨页重复检测无法处理的场景。

#### 3.4.4 图片内文字吸附（CAD/Visio 图）

CAD 软件（Visio/AutoCAD）导出 PDF 常为"位图主体 + 独立文字图层"：图形是位图，文字标签是叠加的独立文本行。吸附规则：

- 文本块 ≤ 40 字符（图内标签通常很短；长正文不吸附）；

- 与图片包围盒**相交**，或中心距 ≤ 35pt（图框边缘标签）；大字号图内主标题放宽到 70pt 且要求与图片纵向重叠（排除图片上方的章节标题）；

- 以句末标点结尾的完整句子不吸附。

命中的文字并入图片块文字，原文本块标记为"已消耗"，不再进入正文流；图片资产仍正常导出。

#### 3.4.5 结构图/矩阵图区域检测

矢量绘制的体系结构图（网格排布的短标签，无位图对象）会被当作普通正文提取，严重污染条目内容。检测算法：

1. **候选**：同页 ≥ 12 个短文本块（1\~10 字符），排除纯符号、编号开头、图题、以及"同行存在长文本块"的正文断行碎片；
2. **中文占比 ≥ 50%**（结构图标签以中文为主；英文表格单元格走表格通道）；
3. **行带结构**：按纵坐标 15pt 聚类成行带，行带数 ≥ 4；
4. **真表格排除**（三重防线）：

   - 连续 ≥ 3 个行带拥有相同块数且块数 ≥ 3（规整网格 = 表格）；

   - 区域内横线与竖线各 ≥ 3 条（有线表格）；

   - 无图题时一律检查线格，有"图X"图题的结构图区域豁免；
5. **页面覆盖守卫**：候选区域高度超过页面主体高度 70% → 是正文页而非局部结构图，跳过；
6. 命中后：区域内所有非图题文本块（含较长的结构图说明文字）合并为一个 figure 块，区域内的位图一并吞并（避免同一张图输出两次）。

**矢量图（LaTeX tikz/pgfplots）检测**：图题上方 9 倍图题高度的区域内矢量线 ≥ 15 条且无位图 → 判为矢量图区域，转 figure 块。

#### 3.4.6 跨页段落合并

中文段落常被分页切断（页末以单字或逗号结尾，下页首行续接）。合并条件（全部满足）：

- 前块是**本页最后一个**正文块，且贴近页底（距本页正文最大纵坐标 ≤ 60pt）；

- 后块是**下一页第一个**正文块，且贴近页顶（≤ 60pt）；

- 两者字体签名完全一致；

- 前段以"未完结"方式结尾（非句末标点；中文或逗号/顿号/分号/冒号结尾）；

- 后段不以编号/项目符号开头（避免把下一节首段合并进来）；

- 前段末字符与后段首字符均为中文（跨页断行通常发生在中文词内部）。

防重复注入：合并过的块登记在册，后续块不得再次合并。几何边界约束保证只有真正的"页末切段"才被合并，页面中部的标题/段落不会被错误吸附。

### 3.5 L3c 表格结构重建

**目标**：提取表格内容为二维单元格网格，并把表格区域从正文流中"挖出"，使表内文字不污染正文段落。

#### 3.5.1 双引擎策略（准确性优先）

| 引擎         | 模式                               | 启用条件                        |
| ---------- | -------------------------------- | --------------------------- |
| Camelot    | lattice（有线表）优先，无结果再试 stream（无线表） | Ghostscript 可用且 camelot 可导入 |
| pdfplumber | lines 策略提取有线表；无线表走自研全页检测         | 始终可用（兜底）                    |

Camelot 准确率 < 60% 时记入警告。pdfplumber 引擎内置**图表区域排除**：表区域内旋转/竖排词 ≥ 2 个或占比 > 30% → 是图表坐标轴而非表格。

#### 3.5.2 无线表自研检测（关键场景：需求追踪关系表）

需求追踪关系表常为无线表，且列数少（2\~3 列）、行数少，通用工具会漏检，导致表内容混入正文、污染条目提取。检测流程：

1. **行分组**：全页词按纵坐标 3pt 聚类成行；
2. **字段切分**：行内词间隙 > 6pt 切分为字段；单字段行（标题/正文/说明）剔除；
3. **分段**：行间距 > 24pt 视为段落边界，切分为候选段；
4. **表头关键词启发**（追踪表专项）：段首行字段命中"条目/编号/序号"之一 **且** 命中"追踪/来源/上游/对应"之一（或命中 ≥ 2 个表头关键词）→ 放宽为"至少 2 行、至少 2 列"即认定为表；
5. **通用段门槛**：无关键词命中时要求段 ≥ 3 行；
6. **网格重建与防误检**（四重防线）：

   - 候选列边界 = 字段起点 8pt 聚类，列数限定 \[2, 12]（双栏正文/公式会聚出 18+ 列被排除）；

   - 每行每个字段起点必须命中某条列边界（± 6pt）；

   - 行字段一致性：众数"唯一列数"必须等于列数（每行占满全部列）；

   - 行距一致性：行间距变异系数 ≤ 0.8（防段落块误检）；
7. **双栏防线**：若检测出的列起点与页面正文栏起点簇完全吻合 → 是双栏正文被误判为表格，剔除。

#### 3.5.3 后处理

- **合并单元格恢复**：提取网格中的空单元格，依据原始单元格包围盒跨多行/列的证据向上/向左延伸；

- **表头识别**：首行（或前 N 行）与数据行存在样式/粗线分隔 → 标记表头行数；

- **跨页接续表拼接**：相邻页表头文本去空白后完全一致 → 拼接两表并去除重复表头；

- **结构校验**：行列一致性、空单元格占比、列边界对齐检查，输出警告。

#### 3.5.4 表格挖出与图重叠删除

- 表格区域从正文块流中挖出：与表包围盒重叠的文本块标记为"已消耗"，其内容归入表格通道，不再进入正文（防止追踪表行被 ID 正则误认为条目）；

- 与结构图/矢量图 figure 块重叠（覆盖比 > 0.3）的"表格"实为误检（结构图被网格化），直接删除。

### 3.6 L3a 标题识别与章节层级树

**目标**：判定哪些块是标题、属于第几级，并以"层级栈"构建章节树——从机制上杜绝"标题把下一节内容吸进来"。

#### 3.6.1 目录页剔除（前置）

工程文档常带目录页，目录条目（"3.2 系统和设备分级 .... 15"）会被编号模式误判为标题，与正文同名章节冲突。识别策略（自底向上，不依赖字体）：

1. 找到"目录 / CONTENTS"标题块；
2. 其后的块若满足"编号开头 + 点线引导符（`...`）或 ≥ 2 连续空格 + 行尾页码"→ 标记为已消耗；
3. 遇到长段落（> 90 字符）或非编号短行 → 目录区结束。

#### 3.6.2 字体签名聚类（标题判定的主证据）

1. **正文基线** = 全文档出现频次最高的字体签名（众数）；
2. **标题候选签名** = 满足以下任一且字号不明显小于正文（≥ 正文 − 2pt）：

   - 字号 > 正文 + 0.5pt；

   - 粗体状态与正文不同；

   - 字体族与正文不同；
3. **频率门槛**：候选签名出现频次须 < max(5, 正文频次 × 0.5)；中文文档中其他 CJK 字体族（黑/楷/宋）放宽到 < max(10, 正文频次)——中文条款标题可高频出现；
4. 显著大字号（≥ 正文 + 1.5pt）无条件保留为候选；
5. **层级排序**：中文按（字号降序、加粗优先）→ 大字号是浅层；西文罕见字体排后 → 深层；相邻候选字号差 ≥ 1.0pt、或粗体不同、或（中文）跨字体族 → 产生新层级；层级上限 6。

#### 3.6.3 标题判定（多重条件 AND）

单个块被判定为标题须同时满足：

| 条件       | 规则                                                                                                                                           |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 独占一行     | 块内 1\~2 行（2 行时总长受限，覆盖 LaTeX "编号行 + 标题行"）                                                                                                     |
| 短文本      | ≤ 80 字符                                                                                                                                      |
| 非断行碎片    | 不以句号/分号/逗号/连字符等结尾                                                                                                                            |
| 有实际文字    | 字母或汉字 ≥ 2 个（过滤纯数字行）                                                                                                                          |
| 非公式行     | 含 =+×÷±√∑∫∞ 等数学符号且非大字号 → 排除                                                                                                                  |
| 标题证据（OR） | ① 编号模式（1 / 1.1 / 1.1.1 / 第X章 / 附录A / A.1 / 1. / 1、 / 1) / (1) / 一、 / （一） / A.）；② 字号 ≥ 正文 + 2pt；③ 全大写加粗（西文，≤ 60 字符）；④ 居中（中心偏差 ≤ 页宽 8%）且字号大于正文 |
| 软否决      | 下一块字号更大且间距 < 6pt → 本块不是标题（是下一块的引导）                                                                                                           |
| 排除段落续行   | 与上一块同字号（± 0.5pt）、起点相近（< 12pt）、同字体族 → 是段落的一部分                                                                                                 |

**编号文本模式兜底**：Office/金山模板导出的 PDF 标题与正文同字体同字号，字体签名无差异可用。此时退回纯文本模式：编号开头 + 短行（≤ 60 字符、≤ 2 行、无句末标点）→ 判为标题（层级 3）。

#### 3.6.4 层级栈归属构建树

按阅读顺序遍历块流，维护"当前打开的标题栈"：

- 遇标题：弹出栈中层级 ≥ 自身的标题，入栈；挂到栈顶父标题之下；

- 遇正文/列表/表格/图片：挂到栈顶标题之下；无打开标题则挂到根。

页眉页脚与已消耗块（目录条目、被表格吸收区域）不入树。章节树是 L4 文档输出的骨架，也是发现层章节单元的内容来源。

### 3.7 L3b 列表识别

**目标**：识别工程文档的各类列表，输出嵌套列表树，保留"列表项"这一后续锚点通道的关键结构。

**三证据识别**：

1. **行首标记模式**（9 类）：数字型（`1.` `1、` `1)` `(1)`）、圈号（①）、中文数字（`一、`）、字母型（`a.` `a)`）、bullet（`•·●◦▪□■*`）、短横线（`- – —`）、单独短横线；
2. **左边界缩进聚类**：悬挂缩进 → 子层级（嵌套树用缩进栈构建）；
3. **序号连续性校验**：有序列表要求序号递增且首项为 1 或 2（允许缺项，如 1,2,4,5）；不连续 → 不是列表（是正文中的编号巧合）。

**块内拆分**：一个块可能混合正文与列表，拆分为 `[引导文, 列表, 引导文, 列表, ...]` 区域序列；连续 ≥ 2 行同类标记构成一个列表区域。

**防误判门槛**（来自回归修复）：① 至少一个列表区域含 ≥ 2 个有效项；② 所有标记必须是常见类型——单字母 + 右括号（如正文括号内的 "B）"）被排除，防止整段连续正文被切成"1 个列表项 + 1 个引导文"导致句子折半。

### 3.8 L3 阅读顺序恢复

**目标**：为多栏、图文混排页面恢复人类阅读顺序。

三步法：

1. **列聚类**：按块左起点聚类分列，起点间隔 > 90pt 开新列；单块列并入相邻列（避免边注/图片被当独立列）；页内块数 < 8 不强制多列；
2. **重叠关系 DAG**：块对之间建立先后关系——垂直重叠（同一水平带）→ 左块在前（右起点距 > 4pt）；不垂直重叠 → 上块在前（且水平投影重叠，容差 8pt）；
3. **拓扑排序**：Kahn 算法 + 最小堆（以绘制序号为稳定性键，同层冲突时优先尊重内容流顺序）；出现环（罕见）时剩余节点按绘制序号追加。

### 3.9 L4 输出

- **文档结果**：元信息（页数、字符数、引擎、警告）+ 大纲（标题层级列表）+ 正文块层级树 + 警告清单；

- **图片资产导出**：图片对象按 xref 导出为 PNG；figure 块（结构图/矢量图区域）整区域渲染为 PNG，完整保留图；

- **媒体锚定**：段落内嵌图片记录锚点（类型/包围盒/序号），保证媒体在正文流中的位置可回溯。

### 3.10 遗留解析引擎

遗留引擎（PyMuPDF 正文 + pdfplumber 表格）在系统中的现存职责：

- 作为新引擎的降级路径（`legacy` 模式或新引擎解析失败时）；

- **追踪关系表的查找与解析**始终复用其函数（接收通用表格数据格式，与解析引擎无关）：在文档附录中定位追踪矩阵表，解析为"下游 ID → (上游文档, 上游引用)"关系列表；

- 文本清洗正则库（全半角、私有区字符、列表标记、章节标题等）与页眉页脚正则库被适配器复用，保证新旧引擎正文口径一致。

### 3.11 解析缓存与并行预取

- **指纹缓存**：以（绝对路径, 修改时间 ns, 文件大小）为键，文件改动自动失效；正文缓存、页区域缓存、表格缓存分别设 LRU 上限（24 份文档 / 4000 页）；

- **并行预取**：多文档处理时用线程池并行预解析（PyMuPDF 页面文本提取期间释放 GIL，可真实并行），随后各步骤直接命中缓存；单文档或线程池不可用时自动退回串行。预取只预热缓存，不改变任何解析结果。

***

## 4. 需求内容提取层

**目标**：从解析后的全文中提取"可追踪的单元"——需求条目（ID 型）与章节（章节型），并建立支持模糊引用的内容索引。

### 4.1 文档类型判别

对**全文**（而非开头片段——部分文档前半是概述、条目集中在后半）执行条目 ID 正则匹配，去重后 ≥ 3 个 → 判为"ID 型文档"，否则"章节型文档"。

条目 ID 通用正则：尖括号内同时包含至少一个字母和一个数字的任意串（如 `<DCS-SyRS001>`、`<FZSDCS34-SyRS005>`），容虑 `<` 后的空格。

### 4.2 条目提取

**切片**：每个条目的内容 = 该 ID 结束位置到下一个 ID 开始位置之间的文本。

**追踪表行噪声过滤**（ID 正则会误命中追踪表里的引用）：内容命中以下任一特征则该次出现是追踪表行，不作为条目正文——

1. 行内嵌另一个尖括号 ID（表格行两列 ID 相邻）；
2. 以"追踪/引用/对应/来源/上游/关联 + 用户需求/系统需求/上游文件/本条"等引用说明开头；
3. 以"附表/表N/序号/本文档中的/需求标志/编号"等表头词开头；
4. 极短（≤ 30 字符）且不以句末标点结尾（表格行摘要，可能跨行断开）。

**表头截断**：正文条目若因切片延伸夹带了追踪表表头行，在表头位置截断。

**同 ID 合并**：同一 ID 多次出现（表格型文档中同一 ID 连续多行描述一个需求）时合并所有出现的内容，逐行去重后拼接——比"保留最长版本"更优，因为表格型文档的最长出现常吞噬相邻 ID 的内容。

**统计过滤**：条目数 > 5 时，按内容长度的中位数过滤——短于 max(20, 中位数 × 15%) 的条目剔除（附录表中的引用条目）；超过中位数 8 倍且 > 500 字符的条目做噪声截断（在连续 3 行非中文低密度处截断，纯中文长条目不截断）。

**首行标题丢弃**：条目号同行尾随的简短标题（≤ 15 字符、无句末标点）若为"通用章节类"（含章节号，或含"概述/要求/功能/描述"等通用词）则丢弃；**条目专有名词标题保留**——如"机柜门开状态"是该条目的匹配关键词，丢弃会导致下游内容缺失关键短语。

### 4.3 章节提取

**匹配**：行首编号（`3.2.2.1` / `3.2` / `4`，容虑编号后句点）+ 标题文本（以汉字或大写字母开头）；内容取到下一章节标题前。

**目录条目剔除**：候选行若满足"短标题（≤ 40 字符）+ 点线引导符或 ≥ 2 连续空格 + 尾部页码"→ 是目录条目，跳过（与 3.6.1 的块级剔除双保险）。

### 4.4 内容清洗（六道工序）

条目与章节内容统一过六道清洗：

1. **空行移除**：消除 PDF 提取产生的多余空行；
2. **粘连拆分**：被全角分号粘连的编号列表项（`4)…；5)…`）拆分回独立行；
3. **架构图文字块移除**（矢量图节点标签污染）：单行判定——以句末标点结尾的完整句子不判；汉字 ≥ 8 个且含中文标点的条款不判；短缩写行（汉字 ≤ 3 且 ≤ 12 字符、含字母）、多斜杠连接行（如 `NC DCS/DAS/ECPRSP/`）、大写缩写标签（PIPS/ESFAC/SVDU 等，汉字 ≤ 5 且 ≤ 30 字符）、专用短标签词（"停堆断路器"、"点对点光纤通信"等，汉字 ≤ 15 且 ≤ 40 字符）判为图内文字。连续 ≥ 5 行图内文字整段删除；"图N…架构示意图"图注行连同其前方连续图内文字删除；
4. **ASCII 图表噪声移除**：含制表/框线字符的行；全 ASCII 短行（≤ 30 字符）含"大写字母连字数字"或以 ≥/≤ 开头；中文占比 < 10% 且多短 token 的行。连续 ≥ 3 行噪声、或噪声行占比 > 30% 的长文本，整块移除；
5. **尾部章节标题剥离**：内容尾部泄漏的下一章节标题（多级编号 + 标题 / 编号独占行 / "紧急停堆系统（RTS）"式子标题，长度 < 60 字符）剥离；
6. **尾部附录引用剥离**："附录A需求追踪…"、"下表A所示…"等尾部引用文本（< 200 字符）剥离。

### 4.5 内容映射与多策略引用解析

每份文档构建内容映射，支持六层递进的引用解析（表格模式中"上游引用 → 上游内容"的解析通道）：

| 顺序 | 策略       | 说明                                                                                      |
| -- | -------- | --------------------------------------------------------------------------------------- |
| 1  | 精确 ID    | 引用串即条目 ID                                                                               |
| 2  | 归一化 ID   | 去空格、统一尖括号后匹配                                                                            |
| 3  | 精确章节号    | 如 `3.2.2.1`                                                                             |
| 4  | 完整键匹配    | "章节号 + 标题"（带/不带空格两种形态）                                                                  |
| 5  | 子串匹配（双向） | 引用串包含于某键，或某键包含于引用串（处理"<ID> 标题"式引用）；含章节号**前缀**匹配（`3.2.2.1` 命中 `3.2.2.1 F-SC1级要求`，保留最长匹配） |
| 6  | 模糊匹配     | 序列相似度 ≥ 0.7 的最佳键                                                                        |

***

## 5. 候选追踪发现层

**目标**：不依赖追踪关系表，在全量上下游单元之间按**内容本身**发现候选追踪关系——回答"这条下游内容最可能源自上游哪一条/哪一节"。

### 5.1 问题定义

**输入 / 输出**：

- 输入：一份下游文档 PDF + 若干上游文档 PDF（流水线 A：单份下游对多份上游；流水线 B：每份设计文档分别对同一份系统需求）；
- 输出：**每个下游单元一条裁决**——`traced`（1 条主关系 + 0~2 条次级关系，每条携带融合置信度与四通道证据）或 `untraced`（空溯源行 + 前 3 名参考候选）。

**三条硬约束**：

1. **不读追踪表**：下游文档附录中的追踪关系表可能缺失、过时或错误，发现模式的全部证据只能来自内容本身；
2. **编号不作证据**：上下游编号体系存在前缀差异（`<FZSDCS34-SyRS005>` vs `<DCS-SyRS005>`）与零填充差异（`0011` vs `011`），编号只在结果登记时作索引，绝不参与打分；
3. **结果必须可解释**：每条关系都要能回答"为什么"——四通道分数摘要与命中锚点原文写入 Excel 证据列，供人工审查。

**问题本质**：对每个下游单元，在全部上游单元（条目 ∪ 章节，可能跨多份文档）中找出内容上承接它的一个或多个单元——一个带"章节-条目"层级结构约束的跨文档最佳配对问题。

**七种真实匹配形态**（算法设计必须覆盖的输入分布）：

| 形态      | 说明                        | 难点              |
| ------- | ------------------------- | --------------- |
| 条目 → 条目 | 一条需求对应另一条需求               | 上游长、下游短，仅部分覆盖   |
| 条目 → 章节 | 下游条目溯源到上游章节               | 上游是纯章节型文档       |
| 一对多     | 一条下游溯源多个上游                | 需输出多条关系         |
| 多对一     | 多条设计条目溯源同一需求              | 多条下游共享术语，难区分    |
| 模板近似    | 多个章节文本高度相似（F-SC1/2/3 级要求） | 整段相似度无法区分，需靠稀有词 |
| 长对短     | 多主题长条目对应单侧面短条目            | 长文本稀释整体相似度      |
| 细化匹配    | 下游是上游的展开细化                | 方向不对称，需识别"谁细化谁" |

### 5.2 设计依据：七轮对照实验

全部设计决策都有实验背书。在 47 条金标关系（流水线 A 16 条 / B1 20 条 / B2 11 条）上共做七轮对照实验，关键观察与对应设计决策：

| 实验观察 | 设计决策 |
| --- | --- |
| 多通道并集召回（实验评测至 Top10）= 100%，三条流水线均是 | 召回宁多勿漏，判别力问题全部交给精排 |
| 锚点通道单通道判别力最强（B2：top1 = 80%、MRR = 0.84） | 锚点作为独立通道保留并重点设计 |
| 但锚点原始分绝对值低（典型 0.03~0.13 量级），线性加权混入会被基础分稀释，判别力消失 | 锚点以**门控 + 加法抬升**接入，不做线性加权 |
| 单元级自然语言推理对发现有害：正确配对的蕴含概率反而极低 | 发现层**禁用 NLI**；推理能力只保留在验证层 |
| 伞形章节吸纳内部条目证据而错误登顶（B1 的"48V 直流电源系统紧急操作台"章节反复挤入无关条目 Top3） | 章节降权 0.85 + 锚点加成仅限条目 + 具体性折叠 |
| 非对称 IDF 中"上游稀有词被下游命中"方向最能刻画"细化"关系 | 词汇通道固定考核该方向 |
| 融合权重网格搜索：语义 0.5 / 包含 0.2 / 稀有词 0.2 / 词频 0.1 综合最优 | 作为默认权重 |

### 5.3 两阶段架构：昂贵预计算 + 廉价裁决

发现引擎分为两个阶段，"昂贵"与"廉价"彻底分离：

```
prepare()（昂贵，每对文档组合只跑一次）
  步骤 1  单元化：PDF 全文 → 下游单元池 + 上游单元池
  步骤 2  文本基础设施：归一化文本 / token 集合 / 句子列表 / 上游锚点池
  步骤 3  两级向量索引：单元向量 + 句向量（批量编码）
  步骤 4  稀有词表（候选池 IDF 统计）+ 词频稠密矩阵
  步骤 5  包含图（章节 → 内部条目）+ 章节降权 / 锚点加成两道掩码
  步骤 6  四通道证据矩阵（全量 下游 × 上游 打分）
  步骤 7  并集召回：每个下游单元一个精比候选集
  步骤 8  句级语义精比：仅对候选集逐对计算不对称句级聚合分
        ↓ 中间产物全部缓存
decide()（廉价，纯查表运算）
  步骤 9   门控融合精排：基础分 + 锚点门控抬升 + 章节降权
  步骤 10  具体性折叠：伞形章节 → 唯一高分内部条目
  步骤 11  裁决：接受 / 未溯源 / 一对多 / 存疑
  步骤 12  证据格式化与矩阵行转换
```

设计意图：步骤 1~8 是 O(下游单元数 × 上游单元数) 的全量计算，一次算好缓存；步骤 9~12 是纯查表运算——调整融合权重、接受阈值、门控门限、折叠容差等任何参数，都可以反复执行裁决而**不重算任何矩阵**。discover 与 hybrid 模式复用同一引擎与同一份缓存。

### 5.4 预计算 · 步骤 1：单元化

**单元**是发现层的基本单位：`（文档名, 类型, 键, 内容）`。条目单元的键 = 条目 ID；章节单元的键 = "章节号 标题"（截断 60 字符）；全局唯一标识 = "文档名#键"。

**角色决定取法**——下游与上游的单元化策略刻意不对称：

| 角色 | 单元来源                |
| -- | ------------------- |
| 下游 | ID 型文档取条目；章节型文档取章节  |
| 上游 | 条目与章节**都取**（上游形态未知） |

**入池门槛与防伪**：

- **长度过滤**：条目内容 ≥ 8 字、章节内容 ≥ 30 字才入池——过短碎片无判别价值；
- **单调骨架过滤**（章节防伪）：正文中存在大量"伪章节号"（表格编号、图注编号、正文回跳编号）。把章节号转为整数元组（`3.2.2.1` → `(3, 2, 2, 1)`），按文档序扫描，仅保留**严格递增**骨架——任何"回跳"的编号剔除；
- 章节单元附文档内顺序号（自 1000 起编），保证后续排序稳定。

### 5.5 预计算 · 步骤 2：文本基础设施

为全部单元预生成四套统一口径的文本视图，供各通道与锚点判定使用：

1. **归一化文本**（与验证层共用同一实现，两层对"文本相等"的判定口径完全一致）：全角→半角、罗马数字→ASCII、剔除 PDF 私有区字符（Wingdings 项目符号）、剔除列表标记与编号前缀（`1)` `a)` `B）`）、剔除全部空白、剔除尾部标点、剔除功能类型词后缀（"功能/系统/模块"等）、高置信字符混淆归一（全大写上下文中 `l→1`）；
2. **token 集合**：ASCII 技术词（字母开头的字母/数字/连字符/斜杠序列，如 `RTC-11`、`GB/T12727`）+ 汉字二元组（相邻两字），统一大写归一——工程文本的判别信息集中在缩写词与技术名词，汉字二元组捕获中文词序；
3. **句子列表**：按 `。；;！!？?` 与换行切分，长度 ≥ 4 字符的句子入列表；
4. **锚点池**（仅上游单元）：逐行匹配列表标记（`-` `•` `●` `1)` `a）` 等）取去编号后的短语；**普通行整行也作锚点候选**（上游需求常以整行短句陈述而非列表形态）；去尾部标点，归一化后 ≥ 3 字符入池，锚点权重 = 归一化长度。

### 5.6 预计算 · 步骤 3：两级向量索引

对全部单元（下游 + 上游统一）建立两级向量索引：

- **短单元**（≤ 300 字符或不可切句）：整条内容直接编码为单元向量；

- **长单元**：按句子边界（。；;！!？?换行）切句，逐句编码；单元向量 = 各句向量按**句长加权**求和后归一化——长文本的语义重心不会被个别短句带偏；

- **句子向量全量补齐**：短单元的句向量也补齐——句级精比（步骤 8）要对候选集内**任意**单元对计算"下游每句 × 上游每句"相似度矩阵，两侧单元都必须有完整句向量，与单元长短无关。

长单元向量公式：`v = Σᵢ len(sᵢ)·v(sᵢ) / Σᵢ len(sᵢ)`——语义重心由长句主导，个别短句不会带偏整体向量。编码全部走批量接口（缓存命中查询 + 未命中合并为大批次，见第 8.2 节），数秒完成全部单元。

### 5.7 预计算 · 步骤 4：稀有词表与词频矩阵

**稀有词表**（稀有词落点率通道的语料）：候选池 = 上下游全部单元，N = 单元总数。统计每个 token 的文档频率 df，df ≤ max(2, N/10) 的 token 为稀有词，权重 = log(N/df) + 1——越罕见权重越高。每个单元持有自己的"稀有词 → 权重"映射。

**词频矩阵**（词频通道的语料）：词表 = 全部 token 排序编号；每个单元的稠密向量中 tf = 1 + log(词频)、idf = log((N+1)/(df+1)) + 1，向量 L2 归一化；下游矩阵 × 上游矩阵转置，一次矩阵乘得到全量词频余弦。

### 5.8 预计算 · 步骤 5：包含图与两道掩码

**包含图**：上游条目的字符二元组 ≥ 70% 落在某上游章节内 → 该条目记为该章节的"内部条目"（children）。它是章节降权与具体性折叠（步骤 10）的基础结构。

由包含图导出两道掩码（长度 = 上游单元数的向量，作用于融合阶段）：

- **章节降权掩码**：含内部条目的章节（"伞形容器"）融合分 × 0.85——否则章节内容会"吸纳"本该属于内部条目的证据而虚高；无内部条目的普通章节不降权；
- **锚点加成掩码**：列表项锚点是"条目"的属性，**任何章节都不享受锚点抬升**（章节位置掩码置 0）——防止伞形章节借内部条目的锚点证据上位（实测曾致 B1 的"48V直流电源系统"章节错误登顶）。

### 5.9 预计算 · 步骤 6：四通道证据矩阵

对全部 下游 × 上游 单元对计算四张打分矩阵：整条语义与词频余弦走矩阵乘（一次算完全量）；短侧包含度、稀有词落点率、锚点命中率逐对计算。所有通道**只用内容**，编号不作证据（编号仅在结果登记时作索引）。

#### 通道一：语义通道（不对称句级聚合）

整条对整条的余弦相似度存在"长文本稀释、短文本被忽略"的问题，改用三特征聚合：

1. **峰值**：所有句对中最相似的一对（捕捉"只覆盖上游一个侧面"）；
2. **短侧覆盖率**：取长度较短的一侧，计算其句子中被对侧承接的比例（句相似度 ≥ 0.60 视为承接，按句长加权）——只考核短侧，长侧剩余部分不扣分；
3. **整条相似度**：传统整体向量余弦。

语义分 = max(整条相似度, 0.4 × 峰值 + 0.6 × 短侧覆盖率)。

max 融合让"整条相近"与"局部强承接"任一成立即可得分——分别覆盖"模板近似"（整条相似、靠稀有词区分，语义分不拖后腿）与"长对短"（局部承接、整条被稀释）两种形态。该聚合较贵（逐对句矩阵运算），召回阶段先用整条余弦，精排阶段（步骤 8）仅对候选集换用本聚合。

#### 通道二：词汇通道（两个互补证据）

- **短侧包含度**：短文本的字符二元组（相邻两字对）有多大比例出现在长文本中——天然适应长短不对称；

- **稀有词落点率（方向性）**：稀有词表已在步骤 4 预计算（候选池 = 上下游全部单元）；打分时**只考核一个方向**——上游的稀有词按权重累计，有多大比例被下游 token 集合命中。这个方向恰好刻画"下游细化上游"的形态，且一个稀有词（如 "F-SC2"、"停堆断路器"）就足以把三个高度相似的模板章节区分开。

#### 通道三：锚点通道（列表项锚定，单通道判别力最强）

针对"设计文档详述需求列表项"场景：

1. 锚点池在步骤 2 预提取（去编号的列表项短语 + 整行短句，归一化后 ≥ 3 字符入池，权重 = 归一化长度）；
2. 检查每个锚点是否被下游**归一化内容**原样包含；不精确包含时，若锚点（≥ 6 字符）的二元组 ≥ 85% 落在下游文本中，按 0.8 权重计近似命中（证据中带 ~ 标记）；
3. 锚点分 = 命中权重 / 总权重（长度加权命中率），并记录命中的锚点原文（写入 Excel 证据列）。

下游长文本只要包含上游的列表项片段，就是极强的溯源证据；且天然区分模板近似章节——实验数据：同一组锚点在正确章节归一化得分 0.96，模板近似的错误章节仅 0.64。

#### 通道四：词频通道（弱信号补充）

经典词频-逆文档频率稠密向量余弦（步骤 4 预计算，矩阵乘一次算完全量）。单独判别力弱，仅以 0.1 小权重参与融合，为前三通道提供词面分布的底线参照。

### 5.10 预计算 · 步骤 7：并集召回

每个下游单元的候选集 = 以下来源之并集：

- 语义、词汇、稀有词、锚点、词频**五个通道**各自的 Top 15（已乘章节降权掩码）；

- 粗排融合分的 Top 30；

- 折叠所需补入：候选章节的内部条目全部补入候选集（供具体性折叠比较）。

召回只依赖通道矩阵，与融合权重无关——调整权重无需重算召回。实验结论：并集召回率三条流水线均为 **100%**，正确答案不会在召回阶段丢失；候选集典型规模在 40 个上游单元以内（远小于全量池），后续句级精比的开销因此可控。

### 5.11 预计算 · 步骤 8：句级语义精比

仅对候选集内的每个（下游, 上游）对，把通道一的整条语义分替换为**不对称句级聚合分**：

1. 取两侧句向量，计算句对余弦矩阵 S（下游句数 × 上游句数）；
2. **峰值** = S 的最大元素；
3. **短侧覆盖率**：按内容长度确定较短一侧；短侧每个句子取其对侧全部句子的最大相似度，≥ 0.60 视为"承接"；覆盖率 = 承接句的句长总和 ÷ 短侧句长总和（句长加权）；
4. 语义精比分 = max(整条相似度, 0.4 × 峰值 + 0.6 × 短侧覆盖率)。

结果按（下游, 候选）缓存，裁决阶段直接查表。**粗精分离控制总计算量**：整条语义在全量对上以一次矩阵乘完成（便宜），句级聚合只在候选集内逐对计算（贵）。

### 5.12 裁决 · 步骤 9：门控融合精排

对每个下游单元的候选集逐个计算融合分（纯查表 + 少量算术）：

```
基础分 = 0.5×语义(句级精比) + 0.2×短侧包含度 + 0.2×稀有词落点率 + 0.1×词频余弦
基础分 ×= 章节降权掩码（伞形章节 ×0.85）

若 归一化锚点分 × 条目掩码 ≥ 0.30（门控）：
    融合分 = 基础分 + 0.5 × 归一化锚点分
否则：
    融合分 = 基础分
融合分截断到 [0, 1]
```

**锚点门控是核心设计**：锚点证据**不**以加权平均混入——若以 0.2 权重线性混合，0.96 的强锚点信号对融合分的贡献仅约 0.19，会被基础分中的噪声淹没（实验验证过线性混合的失效）。锚点原始分绝对值低（典型 0.03~0.13 量级）但判别力强，先除以固定尺度 0.10 归一化到 [0,1] 并截断（保留绝对量级信息；按行归一化会把微小噪声锚点放大成满分强信号——B1"48V 直流电源系统"章节的教训），超过 0.30 门限后以加法独立抬升。

**抬升算例**（真实数据）：正确章节锚点原始分 0.096 → 归一化 0.96 → 门控通过 → 融合分 = 基础分 + 0.48；模板近似的错误章节锚点原始分 0.064 → 归一化 0.64 → 门控通过但仅 +0.32。锚点单项即拉开 0.16 分差，且完全不干扰锚点分低于 0.30 门限的配对。

**证据登记**：精排同时记录每个候选的四通道分数（语义取句级精比分、锚点取原始分）与命中锚点原文——供步骤 12 格式化写入 Excel 证据列。

### 5.13 裁决 · 步骤 10：具体性折叠

融合分降序排序后检查第一名。若第一名是**章节**，检查其内部条目（包含图的 children）中分数与第一名差距 < 0.05（折叠容差）的条目数：

- **恰好 1 个**：把该内部条目提为第一名、原章节移出队列——"折叠"到更具体的粒度；
- **0 个**：章节自身就是答案（无更具体的内部目标），保留；
- **≥ 2 个**：多个内部条目同时接近 → 章节确实是广覆盖的正确粒度（一对多的伞形场景），保留不折叠。

> 直觉：当"章节"和"章节里的某一条"都高分时，答案几乎总是更具体的那一条。

### 5.14 裁决 · 步骤 11：裁决规则

| 规则  | 阈值                            | 输出                                   |
| --- | ----------------------------- | ------------------------------------ |
| 接受  | 第一名 ≥ 0.45                    | 登记为主关系                               |
| 未溯源 | 第一名 < 0.45                    | 空溯源行 + 前 3 名参考候选（写入证据列，供人工审查，而非简单丢弃） |
| 一对多 | 其他候选 ≥ max(0.45, 0.85 × 主关系分) | 追加为次级关系（上限 3 条）                      |
| 存疑  | 第一名与第二名差距 < 0.05              | 标记"待人工确认"，Excel 红色字体                 |

**裁决顺序**：先判接受 / 未溯源 → 接受者扫描次级关系 → 最后计算存疑标记（存疑行仍正常输出关系，只是提示人工复核）。

**弱信号关系的务实处理**：存在一类"架构伞"关系（多条设计条目共同细化同一总体架构需求），它们与干扰候选共享大量结构术语，纯内容算法无法精确排序。系统**不硬猜**：输出存疑多候选 + 验证层着色兜底 + 人工确认标记。

### 5.14A 分级互斥校验（对象一致性）

**问题**：F-SC1级与F-SC2级章节是"模板近似"的极端形态——不同安全等级的设备须满足高度相似的通用要求（单一故障、冗余、在役检查……），五路内容证据天然分不开；而**安全分级代码是"对象标识"而非内容特征**：下游声明 F-SC2 级时，F-SC1 级章节即使逐字相似也是不同对象的要求，不构成正确溯源（实例：`<DCS-SyRS002>` 曾同时挂上 F-SC1/F-SC2 两条关系，且 F-SC1 为主关系）。

**机制**（步骤 9 精排融合阶段施加）：

1. **分级提取**：从下游内容与上游"标题＋内容"中正则提取显式声明的安全分级代码（F-SC1~F-SC4，容错 FSC1/F-SC 1 等写法），归一化为标准形；
2. **互斥判定**：**两侧都显式声明分级且集合完全不相交** → 对象不匹配，融合分 × 0.3（强惩罚）；
3. **交集放行**：任一侧未声明分级（空集）不参与互斥（避免误杀）；下游声明多个分级（如"F-SC1级和F-SC2级功能"）与任一分级的上游交集非空 → 放行（跨级共用要求场景）。

**效果**（流水线 A 实测）：SyRS002 的 F-SC1 候选由 0.88 降至 0.26——低于接受阈值（不进主关系）、低于次级门槛（不进次级关系）、与主关系 F-SC2（0.83）差距远超存疑间隔（不再触发人工/LLM 复核）；SyRS001→F-SC1 不受影响。被拦截候选在证据列标注"分级不符(下游F-SC2 vs 候选F-SC1)"。LLM 裁决器同步增强：系统提示明确"分级代码是对象标识"，提示词注入双方分级声明，裁决确认后组内非首选行标注"AI建议复核"而非沿用确认文案。

### 5.15 证据格式化与矩阵行转换（步骤 12）

**证据格式化**（每条关系的人类可读摘要，写入 Excel 证据列）：

- traced 行：`语义0.xx 包含0.xx 稀有词0.xx 锚点0.xx | 锚点: 命中锚点原文（前 3 条，近似命中带 ~ 标记）`——四通道分数 + 最直接的证据原文；
- untraced 行：`UNTRACED | 参考候选: 文档:条目(分数); …`（前 3 名）——未溯源本身是有价值的审查产出，参考候选为人工复核指路。

**矩阵行转换**：每个下游单元一组（同一序号）——traced 输出主关系行 + 每条次级关系行（一对多合并组，Excel 中下游侧三列纵向合并）；untraced 输出单行空溯源（上游三列留空）。每行携带关系来源（discover）、融合置信度、证据摘要、存疑标记（对应第 9.1 节 11 列结构的后四列）。

### 5.16 完整算例（流水线 A，真实数据）

下游条目 `<DCS-SyRS001>`（系统需求），上游候选含用户需求文档的两个模板近似章节 `3.2.2.1 F-SC1级要求` 与 `3.2.2.2 F-SC2级要求`（章节文本高度相似，仅安全分级与个别术语不同）：

1. **单元化**（步骤 1）：下游为 ID 型文档 → 条目单元；上游为章节型 → 条目 ∪ 章节单元（含两个 F-SC 章节）；
2. **锚点提取**（步骤 2）：`3.2.2.1` 的锚点池含 6 项——"F-SC1级设备执行安全功能，要满足下列(不限于)要求:"、"单一故障准则、冗余、隔离、多样性、故障分析(FMEA)等"、"在役检查和定期试验"、"失去厂外电时仍运行"、"环境和抗震(I类)鉴定"、"仪控部件的硬件鉴定GB/T12727、GB/T13625和RCC-E"；
3. **锚点打分**（步骤 6）：`<DCS-SyRS001>` 内容原样承接其中大部分锚点 → 正确章节锚点原始分 0.096（归一化 0.96）；F-SC2 章节锚点结构相同但"F-SC1"等关键锚点未命中 → 原始分 0.064（归一化 0.64）；
4. **稀有词落点率**（步骤 6）："F-SC1"在候选池内罕见（高权重稀有词）且被下游命中，"F-SC2"未命中——两章节在词汇通道进一步拉开；
5. **并集召回**（步骤 7）：锚点与语义通道均将两个 F-SC 章节召入候选集（并集保证召回）；
6. **门控融合**（步骤 9）：两章节基础分接近（模板近似），锚点归一化 0.96 vs 0.64 均过 0.30 门控，加法抬升 +0.48 vs +0.32 → 实测主关系融合分 0.97；
7. **具体性折叠**（步骤 10）：第一名是章节但无接近的内部条目 → 不折叠；
8. **裁决**（步骤 11）：0.97 ≥ 0.45 → 接受为主关系；其余候选未达 max(0.45, 0.85 × 0.97) → 无次级关系；第一名领先充分 → 不存疑；
9. **输出**（步骤 12）：主行 `<DCS-SyRS001>` ← `3.2.2.1 F-SC1级要求`，置信度 0.97，证据列含四通道分数与命中锚点原文。

**对照算例**（同文档 `<DCS-SyRS002>`，金标 = F-SC2 级要求）：融合分 F-SC1 0.88 / F-SC2 0.83，差距仅 0.05 → 判**存疑**——两条关系都输出、红色字体提示人工确认，且金标关系 F-SC2 以次级关系身份保留在输出中（正确答案未丢失，只是排序不确定）。算法对无法可靠区分的模板对不硬猜，把最终裁决权交给人工——这是发现层"可解释、可审查"原则的直接体现。

***

## 6. 追踪矩阵构建层

### 6.1 表格模式（table）

1. **并行预取**所有 PDF（下游 + 上游）；
2. **解析追踪关系表**：定位下游文档附录中的追踪矩阵表，解析为关系列表（下游 ID, 上游文档名, 上游引用）；流水线 B 中每份设计文档独立解析，支持 Type A/B 两种表结构；
3. **提取下游内容映射**（条目 ID → 内容，含"ID + 标题"式引用的模糊子串回退）；
4. **构建上游内容映射**（第 4.5 节的六策略索引）；
5. **构建矩阵行**：按下游 ID 分组，同一 ID 的多条关系共用一个序号（一对多）；每条关系解析上游引用填充内容——**仅在引用所指文档内解析，不在其他文档回退查找**（防止跨文档内容串稿）。

**ID 核心归一化（跨文档容错）**：不同文档对同一需求的编号存在前缀/零填充差异（`<FZSDCS34-SyRS005>` vs `<DCS-SyRS005>`）。取 ID 中最后一组"字母 + 数字"，字母转小写、数字按整数归一（`syrs5`），作为跨文档可比的核心标识——解析失败时按核心 ID 二次匹配。

**文档名匹配**：追踪表中的上游文档名与实际文档名之间去空格精确匹配 → 子串双向匹配。

### 6.2 发现模式（discover）

跳过追踪表，直接运行候选发现层（第 5 章）：

- 流水线 A：下游整体单元化，与全部上游单元做一次发现；

- 流水线 B：每份设计文档独立跑一遍发现（各自对同一份系统需求），序号跨文档续编，行上记录所属文档名（用于分 sheet）。

裁决转矩阵行：

- **traced**：主关系 + 次级关系各一行（同一序号，一对多合并）；每行携带融合置信度与四通道证据摘要；

- **untraced**：输出空溯源行（上游三列留空），证据列写入"前 3 名参考候选"（文档:条目(分数)）——未溯源本身是有价值的审查产出。

### 6.3 混合模式（hybrid）

以表为准，发现结果做两类干预：

- **补漏（added）**：某下游条目在表中**没有任何**关系，但发现层给出高置信候选 → 追加发现结果为新行，证据列前缀 `[added]`；

- **纠偏（suspicious）**：表中有该条目的关系，但发现层的最优建议与表中关系**不一致**（同文档且引用指向不同单元）→ **保留表中关系**，关系来源列改为 hybrid，证据列写入 `[suspicious] 发现建议 …(分数)`，提示人工复核。

### 6.4 合并组与正向矩阵

- **逆向合并组**：同一下游条目（同序号）的多行，在 Excel 中下游侧三列（序号/条目号/内容）纵向合并；合并组下游内容做**联合着色**（同一字符在多条关系中取最高颜色优先级）；

- **正向转换**：按（上游文档, 上游引用）分组反转，输出"上游条目 → 多条下游条目"的正向视角，上游侧合并，写入独立 Excel 文件。

***

## 7. 微块文本匹配验证层

**目标**：对每一条配对关系做**短语级**内容比对，输出逐字着色（绿=完全一致、蓝=语义匹配、黑=未匹配），既给出整体判定，也精确定位"哪些字句承接了、哪些没有"。

### 7.1 两阶段设计

- **阶段一（粗筛）**：句向量嵌入余弦相似度，快速排除明显不相关的微块对；

- **阶段二（精验）**：字符级指标（二元组 Jaccard / 字符集 Jaccard / 最长公共子序列比率）与自然语言推理模型做最终分类。

### 7.2 预处理与三级微块分解

**ASCII 噪声清理**（匹配前预处理，不影响输出原文）：连续 ≥ 2 行"短纯 ASCII 行/技术标签行"（如 `PIPS-1`、`≥1`）判定为图表噪声剥离，同时维护"清理后位置 → 原文位置"的映射数组——后续所有着色位置都可精确映射回原文。

**三级分解**（段落 → 句子 → 短语）：

1. **段落**：按换行分割；编号列表项（`1)` `a)` `-` `•` 开头）独立成段；
2. **句子**：按句末标点（。；;！!？?）分割；
3. **短语**：按逗号/顿号/冒号/分号/句号/换行/破折号分割；短于 4 字符的碎片与相邻短语合并（保持位置追踪）。

**结构化短语提纯**（用于嵌入编码，提升相似度分辨率）：

- 去除编号前缀（`1)` `a)` `-` `•`）；

- 去除功能类型词后缀（"功能/系统/模块/装置/单元/组件"）——仅对 > 10 字符的短语，且剥离后须 ≥ 3 字符；

- 例：`1)自动停堆功能；` → `自动停堆`。

提纯文本只用于编码与比较，原文通过位置映射保真还原用于着色输出。

### 7.3 嵌入相似度与最优匹配

1. 两侧微块分别用提纯文本批量编码，计算余弦相似度矩阵；
2. **匈牙利算法最优匹配**（替代逐行取最大的贪心）：代价 = 1 − 相似度，相似度 < 0.50 的配对设大代价（不会被选中），全局最小代价的一一匹配——避免多个下游块争抢同一个上游块（贪心的多对一冲突）；scipy 不可用时回退"按相似度降序的约束性贪心"（每块最多被匹配一次）。

### 7.4 分类判定（着色规则）

对每个微块（双向：下游→上游、上游→下游各判一次），按相似度阶梯走如下判定树：

| 顺序 | 条件                                                                                                | 判定                                   |
| -- | ------------------------------------------------------------------------------------------------- | ------------------------------------ |
| 0  | 嵌入相似度 < 0.50                                                                                      | **黑**（直接出局）                          |
| 1  | 嵌入 ≥ 0.95 **且**（字符二元组 Jaccard ≥ 0.92 **或** 字符集 Jaccard ≥ 0.88 且 LCS 比率 ≥ 0.80）**且** 归一化长度比 ≥ 0.70 | **绿**（精确匹配，双路径验证）                    |
| 2  | 两侧均 ≤ 10 字符且原文完全一致                                                                                | **绿**（短文本快速通道，绕过嵌入阈值——解决分解粒度差异导致的漏配） |
| 3  | 嵌入 ≥ 0.85 且归一化后文本完全一致（≥ 4 字符，原始长度比 ≥ 0.50）                                                        | **绿**（后缀微差免疫，如"功能"后缀）                |
| 4  | 嵌入 ≥ 0.70：NLI 矛盾概率 ≥ 0.80                                                                         | **黑**（高置信否定）                         |
| 5  | 嵌入 ≥ 0.70：NLI 蕴含 ≥ 0.85 且字符集 Jaccard ≥ 0.50                                                       | **蓝**（语义承接）                          |
| 6  | 嵌入 ≥ 0.70：NLI 中立 ≥ 0.80 且嵌入 ≥ 0.80 且字符集 Jaccard ≥ 0.50                                            | **蓝**                                |
| 7  | 嵌入 ≥ 0.85 且（LCS ≥ 0.55 或 字符集 Jaccard ≥ 0.60）                                                      | **蓝**（高嵌入回退，无需 NLI）                  |
| 8  | 嵌入 ≥ 0.75 且 字符集 Jaccard ≥ 0.55 且 LCS ≥ 0.45                                                       | **蓝**（分解粒度差异的稀释补偿）                   |
| —  | 以上均不满足                                                                                            | **黑**                                |

其中嵌入相似度 ≥ 0.70 的配对在判定前先做一次**批量 NLI 预热**——把整行所有可能触发 NLI 的文本对收集起来一次前向传播，结果进缓存，逐条判定时全部命中（判定逻辑不变，仅合并小批次为大批次）。

**整体类别** = 该行所有微块中的最高类别（绿 > 蓝 > 黑）。

### 7.5 子短语绿扫描

当短语级匹配为黑，但块级上下文相似度 ≥ 0.45 时，在黑块内部寻找与对侧（Top 5 候选块）**完全一致的子串**：

1. 归一化后做最长公共子串动态规划，子串 ≥ 4 字符为候选；
2. 每个候选须通过字符二元组 Jaccard ≥ 0.92 交叉验证（防误匹配）；
3. 去除被更长子串包含的候选，贪心选择非重叠子串（按长度降序，每块最多 3 个）；
4. 绿色子串总长不超过块文本的 90%（防止几乎全绿）；
5. 子串位置经位置映射转回原文坐标，块以"子短语"粒度参与着色输出——黑块内部被承接的片段精确标绿，间隙保持原类别。

### 7.6 双向一致性与对称化

- **块级一致性**：一侧微块被判绿/蓝时，其对侧的最优匹配块不应为黑——取两者中较高类别统一（绿提升须匹配置信度 ≥ 0.85，防止大块因小块精确匹配被整体标绿）；

- **文本级对称化**：把一侧着色结果中的绿/蓝子串，按"精确子串查找 → 归一化子串查找"两级策略同步到对侧的黑色区域，保证上下游两侧的着色视图一致。

### 7.7 着色输出保真

微块列表 → 着色文本运行的转换中：

- **原文切片保真**：运行文本一律从原文按位置切片重建（不用短语拼接），换行符等空白归属前一个运行，不丢失原文任何字符；

- **子短语交替渲染**：含子短语的块展开为"间隙(原类别) + 绿子串 + 间隙 + …"的交替运行；检测到块位置重叠时回退安全模式（防文本重复）；

- **换行附着**：运行开头的换行符剥离并合并到前一运行末尾——纯换行运行在 WPS/Excel 富文本渲染中可能失效，导致标绿的多行内容被合并成一行。

***

## 8. 模型层

### 8.1 推理设备自适应

设备选择不硬编码算力上限，而是**查询当前 PyTorch 二进制实际编译的架构列表**：

1. 环境变量 `TRACE_NL_DEVICE` 可强制 `cpu` / `cuda`；
2. 自动探测：CUDA 可用 → 读取显卡计算能力（如 sm\_120），须**精确存在**于当前二进制的架构列表中才启用（不依赖 PTX 前向兼容——torch cu118 在 sm\_120 显卡上会因预编译内核退化，反而比 CPU 慢十几倍）；
3. 真实执行一次微型矩阵乘，确认驱动/运行时组合确实可用；
4. 任何环节失败 → 安全回落 CPU（结果全局缓存，只探测一次）。

### 8.2 句向量嵌入模型

- **模型**：BGE-small-zh（中文句向量，本地部署于 `model/embedder`）；

- **编码**：分词 + 注意力掩码 → **掩码平均池化** → L2 归一化；批量编码（批大小 32），单条最大序列 512；

- **缓存**：文本 → 向量的 LRU 缓存，跨行跨文档复用（同一条目在逆向矩阵、正向矩阵、发现层中被反复编码，全部命中缓存）；

- **批量合并**：待编码列表先查缓存，未命中文本去重后合并为大批次一次前向，再按位置组装回原顺序。

### 8.3 自然语言推理模型

- **模型**：Erlangshen-Roberta-110M-NLI（本地部署于 `model/nli`）；

- **输入**：（前提, 假设）有序文本对——方向有意义，故缓存键为有序对；

- **输出**：三分类 softmax 概率（蕴含 / 中立 / 矛盾）；

- **缓存与批量**：同嵌入模型（批大小 16），配合第 7.4 节的批量预热机制。

***

## 9. Excel 报告生成层

### 9.1 逆向追踪矩阵（11 列）

| 列   | 内容                     | 说明                                                      |
| --- | ---------------------- | ------------------------------------------------------- |
| 1–3 | 序号 / 下游条目号 / 下游内容      | 下游内容富文本着色                                               |
| 4–6 | 上游文档 / 上游条目号/章节 / 上游内容 | 上游内容富文本着色                                               |
| 7   | 下游文档                   | 行所属来源文档（多文档分 sheet 依据，正向矩阵识别来源）                         |
| 8   | **关系来源**               | table / discover / hybrid                               |
| 9   | **置信度**                | 发现层融合分（0\~1，表格来源行留空）                                    |
| 10  | **证据**                 | 四通道分数摘要 + 命中锚点短语 / `[added]` / `[suspicious]` / 未溯源参考候选 |
| 11  | **待人工确认**              | 存疑标记（红色字体）                                              |

后四列让每条自动发现的关系**可解释、可审查**——审查者不仅知道"系统认为谁溯源谁"，还知道"为什么"。

**分 sheet**：按下游文档名分组，多文档时每份文档一个 sheet（sheet 名 ≤ 31 字符、去除非法字符），单文档保持"逆向追踪矩阵"。

### 9.2 富文本着色

- 采用单元格富文本（CellRichText + TextBlock + InlineFont），每个着色运行独立拥有颜色与字体（宋体，内容 10 号 / 条目号 11 号）；

- 颜色语义：绿 `00B050`（完全一致）、蓝 `0070C0`（语义匹配）、黑 `000000`（未匹配）；表头蓝底白字加粗，全表细边框、顶端对齐、自动换行；

- **合并组联合着色**：一对多合并组的首行下游内容，把组内所有行的下游着色结果按字符位置叠加，取最高颜色优先级（绿 > 蓝 > 黑）——多条关系对同一下游内容的覆盖情况一图可见。

### 9.3 合并单元格与行高

- 合并组下游侧三列纵向合并；上游内容各行独立着色；

- **行高估算**：按列宽估算换行后的可见行数——可用字符空间 = 列宽 − 2 单位，全角/CJK 字符按 2 单位计，显式换行强制分段；行高 = 行数 × 17pt + 4pt 余量；合并区域总高度按行数平均分摊。

### 9.4 正向追踪矩阵

独立 Excel 文件（7 列：序号 / 上游条目号 / 上游内容 / 上游文档 / 下游条目号 / 下游内容 / 追踪匹配说明），由逆向矩阵按（上游文档, 上游引用）分组反转生成；上游侧三列合并，"追踪匹配说明"列输出整体类别的文字描述（一致 / 部分匹配 / 不一致）。

***

## 10. 性能优化设计

| 机制       | 层  | 设计                                               |
| -------- | -- | ------------------------------------------------ |
| PDF 指纹缓存 | 解析 | 键 =（路径, mtime\_ns, 大小），改动自动失效；正文/页区域/表格三类 LRU 缓存 |
| 新引擎双层缓存  | 解析 | 结构化文档树 + 分页文本双份缓存，指纹键同上                          |
| 并行预取     | 解析 | 线程池并行预解析多文档（PyMuPDF 提取期释放 GIL），后续步骤全部命中缓存        |
| 嵌入缓存     | 模型 | 文本 → 向量 LRU 缓存，跨阶段复用                             |
| NLI 缓存   | 模型 | 有序文本对 → 概率缓存                                     |
| 归一化缓存    | 匹配 | 文本归一化纯函数缓存（10 万条上限）                              |
| 相似度缓存    | 匹配 | 三个字符级指标均为对称函数，用有序对做键，命中率翻倍（20 万条上限）              |
| NLI 批量预热 | 匹配 | 整行可能触发 NLI 的对一次批量前向，逐条判定全命中缓存                    |
| 全矩阵预热    | 匹配 | 逐行匹配前对整个矩阵做一次全局批量编码 + 匹配 + NLI，把小批次合并为大批次        |
| 发现层预计算   | 发现 | 编码/通道矩阵/候选集/句级精比一次性算好缓存；融合与裁决纯查表，调参零重算           |

***

## 11. 关键参数总表

### 11.1 匹配阈值（验证层）

| 参数                | 取值                 | 说明             |
| ----------------- | ------------------ | -------------- |
| 字符二元组 Jaccard（精确） | 0.92               | 绿色判定主路径        |
| 嵌入相似度（精确档）        | 0.95               | 进入绿色验证的门槛      |
| 嵌入相似度（语义档）        | 0.70               | 进入 NLI 验证的门槛   |
| 嵌入相似度（最低门槛）       | 0.50               | 低于直接黑          |
| NLI 蕴含阈值          | 0.85               | 蕴含概率达标判蓝       |
| NLI 矛盾阈值          | 0.80               | 矛盾概率达标判黑       |
| NLI 中立 + 嵌入联合     | 0.80 / 0.80        | 中立高且嵌入高判蓝      |
| 短语最小字符数           | 4                  | 微块分解下限         |
| 子短语绿扫描块级门槛        | 0.45               | 低于不扫描（防跨语义上下文） |
| 子短语绿交叉验证          | 二元组 Jaccard ≥ 0.92 | 防误匹配           |
| 每块绿子串上限           | 3 个                | 且总长 ≤ 块文本 90%  |
| 绿提升置信度            | 0.85               | 双向一致性中绿提升所需相似度 |

### 11.2 发现层参数

| 参数                 | 取值                    | 说明                       |
| ------------------ | --------------------- | ------------------------ |
| 融合权重（语义/包含/稀有词/词频） | 0.5 / 0.2 / 0.2 / 0.1 | 网格搜索所得                   |
| 接受阈值               | 0.45                  | 低于判未溯源                   |
| 存疑间隔               | 0.05                  | 前两名差距小于此值标记存疑            |
| 一对多上限              | 3                     | 单条下游最多关系数                |
| 次级关系比例             | 0.85                  | 次级分 ≥ max(0.45, 0.85×主分) |
| 锚点门限 / 加成 / 归一化尺度  | 0.30 / 0.50 / 0.10    | 门控式接入，防稀释                |
| 章节降权系数             | 0.85                  | 仅含条目的伞形章节                |
| 折叠容差               | 0.05                  | 内部条目与章节分的允许差距            |
| 单通道粗召回数            | 15                    | 每通道保留前 15 名进并集           |
| 粗排融合召回数            | 30（15×2）              | 粗排融合分 Top30 进并集          |
| 句承接阈值（短侧覆盖率）       | 0.60                  | 句相似度达标视为承接               |
| 近似锚点命中             | 二元组 ≥ 0.85，权重 ×0.8    | 锚点非精确包含时                 |
| 包含图门槛              | 条目二元组 ≥ 70% 落入章节      | 章节的"内部条目"判定              |
| 单元长度门槛             | 条目 ≥ 8 字 / 章节 ≥ 30 字  | 入池下限                     |
| 分级互斥惩罚系数           | 0.3                    | 双方显式声明且完全不相交的分级 → 融合分×0.3 |

### 11.3 解析层阈值（节选）

| 参数     | 取值                                   | 说明                |
| ------ | ------------------------------------ | ----------------- |
| 文本层判别  | 每页 ≥ 60 字符、文本页占比 ≥ 70%               | L0 抽样 ≤ 10 页      |
| 段落断段   | 行距 > 1.6×行高；起点差 > 32pt；字号差 > 1.2pt   | L2 行聚类            |
| 页眉页脚   | 跨页 ≥ max(3, 页数×35%) 次 且 y0 极差 ≤ 20pt | L2 行级识别           |
| 图片文字吸附 | ≤ 40 字符，相交或中心距 ≤ 35pt                | CAD 图层            |
| 结构图区域  | ≥ 12 短块、≥ 4 行带、中文 ≥ 50%、覆盖 < 70% 页高  | 含表格三重排除           |
| 无线表网格  | 列数 \[2,12]、列对齐 ±6pt、行距 CV ≤ 0.8      | 追踪表头关键词放宽 2 行 2 列 |
| 跨页段落合并 | 页底/页顶 60pt 内、字体签名一致、中文连续             | 防重复注入             |
| 列表连续性  | 序号递增且首项 ∈ {1, 2}                     | ≥ 2 有效项           |
| 标题长度   | ≤ 80 字符、1\~2 行                       | 编号/大字号/居中证据       |
| 目录条目   | 编号 + 点线/连续空格 + 尾页码                   | 块级与行级双保险          |

***

## 12. 工程结构与运行入口

### 12.1 模块清单

```
Trace_NL/
├─ config.py                    全局配置：路径、正则、阈值、颜色、枚举
├─ app.py                       Streamlit 界面（模式选择、文档选择、结果预览）
├─ run_e2e_test.py              流水线 A 端到端脚本（默认 discover 模式）
├─ run_e2e_sd_test.py           流水线 B 端到端脚本
├─ generate_forward_matrix.py   流水线 A 正向矩阵独立生成
├─ generate_forward_sd_matrix.py 流水线 B 正向矩阵独立生成
├─ compare_detail.py            基准对比（逆向矩阵逐单元格）
├─ compare_row_detail.py        基准对比（行级差异明细）
├─ core/
│  ├─ pdf_parser.py             遗留解析引擎（正文 + 表格 + 追踪表解析 + 缓存）
│  ├─ pdf_parser_adapter.py     双引擎适配器（后端选择、降级、双层缓存）
│  ├─ requirement_extractor.py  条目/章节提取与内容映射
│  ├─ candidate_discovery.py    候选追踪发现引擎
│  ├─ traceability_matrix.py    追踪矩阵构建（三模式、两入口）
│  ├─ text_matcher.py           微块匹配验证引擎
│  ├─ excel_generator.py        Excel 富文本报告
│  └─ debug_dump.py             中间产物 dump（排查解析/提取问题）
├─ pdfparser/                   新五层解析管道
│  ├─ preprocess.py / extract.py / layout.py / tables.py
│  ├─ headings.py / lists.py / order.py / media.py
│  ├─ document.py（编排）/ models.py（数据结构）/ output.py / cli.py
├─ models/
│  ├─ embedding_model.py        BGE-small-zh 封装（缓存 + 批量）
│  ├─ nli_model.py              NLI 模型封装（缓存 + 批量）
│  └─ device_utils.py           推理设备自适应
├─ 系统需求/ 用户需求/ 系统设计/   文档目录
├─ 基准数据/Trace_Base.xlsx      人工标注基准（回归对比）
└─ output/                      带时间戳的输出目录
```

### 12.2 运行方式

```
# 流水线 A（系统需求 → 用户需求），默认 discover
python run_e2e_test.py [table|discover|hybrid]

# 流水线 B（系统设计 → 系统需求）
python run_e2e_sd_test.py [table|discover|hybrid]

# 环境变量
$env:TRACE_NL_PDF_BACKEND="auto|new|legacy"   # 解析后端
$env:TRACE_NL_DEVICE="cpu|cuda"               # 强制推理设备
```

端到端脚本依次执行：构建矩阵 → 微块匹配验证 → 生成逆向 Excel → 生成正向 Excel → 与基准文件逐单元格对比（回归验证）。界面（app.py）提供同样的模式选择与文档上传。

### 12.3 排查支持

各阶段中间产物可 dump 落盘（需求条目、章节、追踪关系、内容映射、提取摘要），配合解析引擎的警告清单，可逐环节定位"解析偏差 → 提取偏差 → 配对偏差"的根源。

***

## 13. 边界与后续方向

**本阶段刻意不做的事**（均有实验依据）：

- **发现层禁用自然语言推理**：单元级推理判断对发现有害（正确配对的蕴含概率反而极低），推理能力只保留在验证层；

- **扫描件 OCR**：L0 判定扫描型后仅输出报告，OCR 通道为独立研究方向（可行性研究已完成）；

- **颜色细分**：基准中的浅绿/浅蓝/红/琥珀等细分颜色语义留待后续对齐。

**后续迭代方向**：

1. 验证层着色结果**回喂**发现层排序（二阶段精排），改善架构伞关系的排序；
2. 流水线 B 的金标扩充与阈值精调；
3. 验证层颜色细分，完整对齐基准颜色语义；
4. 扫描件 OCR 通道集成。

***

## 附：设计原则小结

1. **分层正交**：解析、提取、发现、验证、报告各自独立演进，接口稳定；
2. **内容优先**：追踪表是"参考答案"而非"运行前提"，三种模式平滑共存；
3. **可解释可审查**：每条自动关系携带置信度与证据，未溯源与存疑诚实标注而非硬猜；
4. **防污染设计贯穿始终**：页眉页脚、追踪表行、架构图标签、ASCII 图表、目录条目——每一类噪声都有专门的识别与剔除机制；
5. **工程鲁棒性**：双引擎降级、设备自适应、多重缓存与批量推理、位置映射保真——准确性优先，性能与可靠性并重。

