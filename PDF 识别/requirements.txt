# pdfparser 部署依赖（已适配本地 E:\Trace_NL 项目环境）
#
# 原项目 README 安装清单为:
#   pymupdf pdfminer.six pdfplumber pandas numpy opencv-python-headless
#   (camelot-py 可选, 需要 Ghostscript)
#
# 适配策略: 不再独立安装依赖, 全部复用本地 E:\Trace_NL 项目已安装的包。
# 本机实际校验版本 (pip list, 2026-08-17):
#   PyMuPDF                1.24.11
#   pdfminer.six           20231228
#   pdfplumber             0.11.5
#   pandas                 2.3.3
#   numpy                  2.2.6
#   opencv-python-headless 4.13.0.92
#   pytest                 9.0.3
#
# 与本地环境不同的两处调整及其影响 (详见 环境依赖调整说明.md):
#   1. camelot-py 未安装 (本地环境没有) -> 表格引擎自动降级为 pdfplumber
#      自研网格重建, 属项目设计内的降级路径, 全部样本通过。
#   2. Ghostscript 未安装 -> camelot 无法运行, 同样走降级路径。
#
# 若需恢复 camelot 高准确率表格引擎, 可另行:
#   pip install camelot-py
#   # 并安装 Ghostscript (gs) 并加入 PATH
# 但注意 camelot-py 依赖 pandas/numpy/opencv-python, 本地版本均已满足。

PyMuPDF>=1.24.0
pdfminer.six>=20231228
pdfplumber>=0.11.0
pandas>=2.0
numpy>=1.24
opencv-python-headless>=4.8
# 测试依赖
pytest>=7.0
